type Role {
  id: String!
  name: String!
  description: String!
  contract: RolesContractEnum!
  prompt: String!
  createdAt: DateTime!
  updatedAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  lanes(
    where: LaneFilters
    orderBy: LaneOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [LaneDistinctColumn!]
  ): [Lane!]!
  lanesAggregate(where: LaneFilters): LaneAggregate!
}

enum RolesContractEnum {
  """Value: work"""
  work

  """Value: verdict"""
  verdict

  """Value: expand"""
  expand
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

"""Columns of Lane that a query can be made distinct on"""
enum LaneDistinctColumn {
  id
  projectId
  name
  position
  intake
  roleId
  prompt
  agentId
  onSuccessLaneId
  archiveOnSuccess
  onFailureLaneId
  wipLimit
  maxAttempts
  createdAt
}

type RoleAggregate {
  count: Int!
  min: RoleMinAggregate
  max: RoleMaxAggregate
  countNonNull: RoleCountNonNullAggregate
  countDistinct: RoleCountDistinctAggregate
}

type RoleMinAggregate {
  id: String
  name: String
  description: String
  contract: RolesContractEnum
  prompt: String
  createdAt: DateTime
  updatedAt: DateTime
}

type RoleMaxAggregate {
  id: String
  name: String
  description: String
  contract: RolesContractEnum
  prompt: String
  createdAt: DateTime
  updatedAt: DateTime
}

type RoleCountNonNullAggregate {
  id: Int!
  name: Int!
  description: Int!
  contract: Int!
  prompt: Int!
  createdAt: Int!
  updatedAt: Int!
}

type RoleCountDistinctAggregate {
  id: Int!
  name: Int!
  description: Int!
  contract: Int!
  prompt: Int!
  createdAt: Int!
  updatedAt: Int!
}

type RoleGroupBy {
  group: RoleGroupKeys!
  count: Int!
  min: RoleMinAggregate
  max: RoleMaxAggregate
  countNonNull: RoleCountNonNullAggregate
  countDistinct: RoleCountDistinctAggregate
}

"""
The grouped column values of one Role group. A column the query did not group by is null.
"""
type RoleGroupKeys {
  id: String
  name: String
  description: String
  contract: RolesContractEnum
  prompt: String
  createdAt: DateTime
  updatedAt: DateTime
}

type Agent {
  id: String!
  name: String!
  enabled: Boolean!
  baseUrl: String!
  model: String!
  systemPrompt: String!
  maxTokens: Int!
  temperature: Float!
  maxToolIterations: Int!
  toolDiscovery: AgentsToolDiscoveryEnum!
  requestTimeoutSeconds: Int!
  maxRetries: Int!
  createdAt: DateTime!
  updatedAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  servers(
    where: AgentServerFilters
    orderBy: AgentServerOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [AgentServerDistinctColumn!]
  ): [AgentServer!]!
  serversAggregate(where: AgentServerFilters): AgentServerAggregate!
  lanes(
    where: LaneFilters
    orderBy: LaneOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [LaneDistinctColumn!]
  ): [Lane!]!
  lanesAggregate(where: LaneFilters): LaneAggregate!
  runs(
    where: RunFilters
    orderBy: RunOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [RunDistinctColumn!]
  ): [Run!]!
  runsAggregate(where: RunFilters): RunAggregate!
}

enum AgentsToolDiscoveryEnum {
  """Value: inherit"""
  inherit

  """Value: eager"""
  eager

  """Value: ondemand"""
  ondemand
}

"""Columns of AgentServer that a query can be made distinct on"""
enum AgentServerDistinctColumn {
  id
  agentId
  serverId
}

"""Columns of Run that a query can be made distinct on"""
enum RunDistinctColumn {
  id
  projectId
  agentId
  kind
  taskId
  cardId
  laneId
  status
  verdict
  startedAt
  finishedAt
  output
  error
  toolCalls
  promptTokens
  completionTokens
  totalTokens
}

type AgentAggregate {
  count: Int!
  avg: AgentAvgAggregate
  sum: AgentSumAggregate
  min: AgentMinAggregate
  max: AgentMaxAggregate
  countNonNull: AgentCountNonNullAggregate
  countDistinct: AgentCountDistinctAggregate
}

type AgentAvgAggregate {
  maxTokens: Float
  temperature: Float
  maxToolIterations: Float
  requestTimeoutSeconds: Float
  maxRetries: Float
}

type AgentSumAggregate {
  maxTokens: Float
  temperature: Float
  maxToolIterations: Float
  requestTimeoutSeconds: Float
  maxRetries: Float
}

type AgentMinAggregate {
  id: String
  name: String
  baseUrl: String
  model: String
  systemPrompt: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: AgentsToolDiscoveryEnum
  requestTimeoutSeconds: Int
  maxRetries: Int
  createdAt: DateTime
  updatedAt: DateTime
}

type AgentMaxAggregate {
  id: String
  name: String
  baseUrl: String
  model: String
  systemPrompt: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: AgentsToolDiscoveryEnum
  requestTimeoutSeconds: Int
  maxRetries: Int
  createdAt: DateTime
  updatedAt: DateTime
}

type AgentCountNonNullAggregate {
  id: Int!
  name: Int!
  enabled: Int!
  baseUrl: Int!
  model: Int!
  systemPrompt: Int!
  maxTokens: Int!
  temperature: Int!
  maxToolIterations: Int!
  toolDiscovery: Int!
  requestTimeoutSeconds: Int!
  maxRetries: Int!
  createdAt: Int!
  updatedAt: Int!
}

type AgentCountDistinctAggregate {
  id: Int!
  name: Int!
  baseUrl: Int!
  model: Int!
  systemPrompt: Int!
  maxTokens: Int!
  temperature: Int!
  maxToolIterations: Int!
  toolDiscovery: Int!
  requestTimeoutSeconds: Int!
  maxRetries: Int!
  createdAt: Int!
  updatedAt: Int!
}

type AgentGroupBy {
  group: AgentGroupKeys!
  count: Int!
  avg: AgentAvgAggregate
  sum: AgentSumAggregate
  min: AgentMinAggregate
  max: AgentMaxAggregate
  countNonNull: AgentCountNonNullAggregate
  countDistinct: AgentCountDistinctAggregate
}

"""
The grouped column values of one Agent group. A column the query did not group by is null.
"""
type AgentGroupKeys {
  id: String
  name: String
  baseUrl: String
  model: String
  systemPrompt: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: AgentsToolDiscoveryEnum
  requestTimeoutSeconds: Int
  maxRetries: Int
  createdAt: DateTime
  updatedAt: DateTime
  enabled: Boolean
}

type McpServer {
  id: String!
  slug: String!
  label: String!
  enabled: Boolean!
  transport: McpServersTransportEnum!
  command: String!
  args: JSON
  env: JSON
  url: String!
  headers: JSON

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  agents(
    where: AgentServerFilters
    orderBy: AgentServerOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [AgentServerDistinctColumn!]
  ): [AgentServer!]!
  agentsAggregate(where: AgentServerFilters): AgentServerAggregate!
}

enum McpServersTransportEnum {
  """Value: stdio"""
  stdio

  """Value: http"""
  http
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON @specifiedBy(url: "http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf")

type McpServerAggregate {
  count: Int!
  min: McpServerMinAggregate
  max: McpServerMaxAggregate
  countNonNull: McpServerCountNonNullAggregate
  countDistinct: McpServerCountDistinctAggregate
}

type McpServerMinAggregate {
  id: String
  slug: String
  label: String
  transport: McpServersTransportEnum
  command: String
  url: String
}

type McpServerMaxAggregate {
  id: String
  slug: String
  label: String
  transport: McpServersTransportEnum
  command: String
  url: String
}

type McpServerCountNonNullAggregate {
  id: Int!
  slug: Int!
  label: Int!
  enabled: Int!
  transport: Int!
  command: Int!
  args: Int!
  env: Int!
  url: Int!
  headers: Int!
}

type McpServerCountDistinctAggregate {
  id: Int!
  slug: Int!
  label: Int!
  transport: Int!
  command: Int!
  url: Int!
}

type McpServerGroupBy {
  group: McpServerGroupKeys!
  count: Int!
  min: McpServerMinAggregate
  max: McpServerMaxAggregate
  countNonNull: McpServerCountNonNullAggregate
  countDistinct: McpServerCountDistinctAggregate
}

"""
The grouped column values of one McpServer group. A column the query did not group by is null.
"""
type McpServerGroupKeys {
  id: String
  slug: String
  label: String
  transport: McpServersTransportEnum
  command: String
  url: String
  enabled: Boolean
}

type AgentServer {
  id: String!
  agentId: String!
  serverId: String!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  agent(where: AgentFilters): Agent!
  server(where: McpServerFilters): McpServer!
}

type AgentServerAggregate {
  count: Int!
  min: AgentServerMinAggregate
  max: AgentServerMaxAggregate
  countNonNull: AgentServerCountNonNullAggregate
  countDistinct: AgentServerCountDistinctAggregate
}

type AgentServerMinAggregate {
  id: String
  agentId: String
  serverId: String
}

type AgentServerMaxAggregate {
  id: String
  agentId: String
  serverId: String
}

type AgentServerCountNonNullAggregate {
  id: Int!
  agentId: Int!
  serverId: Int!
}

type AgentServerCountDistinctAggregate {
  id: Int!
  agentId: Int!
  serverId: Int!
}

type AgentServerGroupBy {
  group: AgentServerGroupKeys!
  count: Int!
  min: AgentServerMinAggregate
  max: AgentServerMaxAggregate
  countNonNull: AgentServerCountNonNullAggregate
  countDistinct: AgentServerCountDistinctAggregate
}

"""
The grouped column values of one AgentServer group. A column the query did not group by is null.
"""
type AgentServerGroupKeys {
  id: String
  agentId: String
  serverId: String
}

type Project {
  id: String!
  name: String!
  description: String!
  context: String!
  autoRun: Boolean!
  refineAgentId: String
  createdAt: DateTime!
  updatedAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  lanes(
    where: LaneFilters
    orderBy: LaneOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [LaneDistinctColumn!]
  ): [Lane!]!
  lanesAggregate(where: LaneFilters): LaneAggregate!
  tasks(
    where: TaskFilters
    orderBy: TaskOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [TaskDistinctColumn!]
  ): [Task!]!
  tasksAggregate(where: TaskFilters): TaskAggregate!
  cards(
    where: CardFilters
    orderBy: CardOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [CardDistinctColumn!]
  ): [Card!]!
  cardsAggregate(where: CardFilters): CardAggregate!
  runs(
    where: RunFilters
    orderBy: RunOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [RunDistinctColumn!]
  ): [Run!]!
  runsAggregate(where: RunFilters): RunAggregate!
  refineAgent(where: AgentFilters): Agent
}

"""Columns of Task that a query can be made distinct on"""
enum TaskDistinctColumn {
  id
  projectId
  title
  brief
  createdAt
  updatedAt
}

"""Columns of Card that a query can be made distinct on"""
enum CardDistinctColumn {
  id
  projectId
  taskId
  parentId
  laneId
  title
  body
  acceptance
  position
  status
  error
  attempts
  archivedAt
  createdAt
  updatedAt
}

type ProjectAggregate {
  count: Int!
  min: ProjectMinAggregate
  max: ProjectMaxAggregate
  countNonNull: ProjectCountNonNullAggregate
  countDistinct: ProjectCountDistinctAggregate
}

type ProjectMinAggregate {
  id: String
  name: String
  description: String
  context: String
  refineAgentId: String
  createdAt: DateTime
  updatedAt: DateTime
}

type ProjectMaxAggregate {
  id: String
  name: String
  description: String
  context: String
  refineAgentId: String
  createdAt: DateTime
  updatedAt: DateTime
}

type ProjectCountNonNullAggregate {
  id: Int!
  name: Int!
  description: Int!
  context: Int!
  autoRun: Int!
  refineAgentId: Int!
  createdAt: Int!
  updatedAt: Int!
}

type ProjectCountDistinctAggregate {
  id: Int!
  name: Int!
  description: Int!
  context: Int!
  refineAgentId: Int!
  createdAt: Int!
  updatedAt: Int!
}

type ProjectGroupBy {
  group: ProjectGroupKeys!
  count: Int!
  min: ProjectMinAggregate
  max: ProjectMaxAggregate
  countNonNull: ProjectCountNonNullAggregate
  countDistinct: ProjectCountDistinctAggregate
}

"""
The grouped column values of one Project group. A column the query did not group by is null.
"""
type ProjectGroupKeys {
  id: String
  name: String
  description: String
  context: String
  refineAgentId: String
  createdAt: DateTime
  updatedAt: DateTime
  autoRun: Boolean
}

type Lane {
  id: String!
  projectId: String!
  name: String!
  position: Int!
  intake: Boolean!
  roleId: String
  prompt: String!
  agentId: String
  onSuccessLaneId: String
  archiveOnSuccess: Boolean!
  onFailureLaneId: String
  wipLimit: Int!
  maxAttempts: Int!
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  project(where: ProjectFilters): Project!
  role(where: RoleFilters): Role
  agent(where: AgentFilters): Agent
  cards(
    where: CardFilters
    orderBy: CardOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [CardDistinctColumn!]
  ): [Card!]!
  cardsAggregate(where: CardFilters): CardAggregate!
}

type LaneAggregate {
  count: Int!
  avg: LaneAvgAggregate
  sum: LaneSumAggregate
  min: LaneMinAggregate
  max: LaneMaxAggregate
  countNonNull: LaneCountNonNullAggregate
  countDistinct: LaneCountDistinctAggregate
}

type LaneAvgAggregate {
  position: Float
  wipLimit: Float
  maxAttempts: Float
}

type LaneSumAggregate {
  position: Float
  wipLimit: Float
  maxAttempts: Float
}

type LaneMinAggregate {
  id: String
  projectId: String
  name: String
  position: Int
  roleId: String
  prompt: String
  agentId: String
  onSuccessLaneId: String
  onFailureLaneId: String
  wipLimit: Int
  maxAttempts: Int
  createdAt: DateTime
}

type LaneMaxAggregate {
  id: String
  projectId: String
  name: String
  position: Int
  roleId: String
  prompt: String
  agentId: String
  onSuccessLaneId: String
  onFailureLaneId: String
  wipLimit: Int
  maxAttempts: Int
  createdAt: DateTime
}

type LaneCountNonNullAggregate {
  id: Int!
  projectId: Int!
  name: Int!
  position: Int!
  intake: Int!
  roleId: Int!
  prompt: Int!
  agentId: Int!
  onSuccessLaneId: Int!
  archiveOnSuccess: Int!
  onFailureLaneId: Int!
  wipLimit: Int!
  maxAttempts: Int!
  createdAt: Int!
}

type LaneCountDistinctAggregate {
  id: Int!
  projectId: Int!
  name: Int!
  position: Int!
  roleId: Int!
  prompt: Int!
  agentId: Int!
  onSuccessLaneId: Int!
  onFailureLaneId: Int!
  wipLimit: Int!
  maxAttempts: Int!
  createdAt: Int!
}

type LaneGroupBy {
  group: LaneGroupKeys!
  count: Int!
  avg: LaneAvgAggregate
  sum: LaneSumAggregate
  min: LaneMinAggregate
  max: LaneMaxAggregate
  countNonNull: LaneCountNonNullAggregate
  countDistinct: LaneCountDistinctAggregate
}

"""
The grouped column values of one Lane group. A column the query did not group by is null.
"""
type LaneGroupKeys {
  id: String
  projectId: String
  name: String
  position: Int
  roleId: String
  prompt: String
  agentId: String
  onSuccessLaneId: String
  onFailureLaneId: String
  wipLimit: Int
  maxAttempts: Int
  createdAt: DateTime
  intake: Boolean
  archiveOnSuccess: Boolean
}

type Task {
  id: String!
  projectId: String!
  title: String!
  brief: String!
  createdAt: DateTime!
  updatedAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  project(where: ProjectFilters): Project!
  messages(
    where: MessageFilters
    orderBy: MessageOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [MessageDistinctColumn!]
  ): [Message!]!
  messagesAggregate(where: MessageFilters): MessageAggregate!
  cards(
    where: CardFilters
    orderBy: CardOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [CardDistinctColumn!]
  ): [Card!]!
  cardsAggregate(where: CardFilters): CardAggregate!
}

"""Columns of Message that a query can be made distinct on"""
enum MessageDistinctColumn {
  id
  taskId
  role
  content
  createdAt
}

type TaskAggregate {
  count: Int!
  min: TaskMinAggregate
  max: TaskMaxAggregate
  countNonNull: TaskCountNonNullAggregate
  countDistinct: TaskCountDistinctAggregate
}

type TaskMinAggregate {
  id: String
  projectId: String
  title: String
  brief: String
  createdAt: DateTime
  updatedAt: DateTime
}

type TaskMaxAggregate {
  id: String
  projectId: String
  title: String
  brief: String
  createdAt: DateTime
  updatedAt: DateTime
}

type TaskCountNonNullAggregate {
  id: Int!
  projectId: Int!
  title: Int!
  brief: Int!
  createdAt: Int!
  updatedAt: Int!
}

type TaskCountDistinctAggregate {
  id: Int!
  projectId: Int!
  title: Int!
  brief: Int!
  createdAt: Int!
  updatedAt: Int!
}

type TaskGroupBy {
  group: TaskGroupKeys!
  count: Int!
  min: TaskMinAggregate
  max: TaskMaxAggregate
  countNonNull: TaskCountNonNullAggregate
  countDistinct: TaskCountDistinctAggregate
}

"""
The grouped column values of one Task group. A column the query did not group by is null.
"""
type TaskGroupKeys {
  id: String
  projectId: String
  title: String
  brief: String
  createdAt: DateTime
  updatedAt: DateTime
}

type Message {
  id: String!
  taskId: String!
  role: MessagesRoleEnum!
  content: String!
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  task(where: TaskFilters): Task!
}

enum MessagesRoleEnum {
  """Value: user"""
  user

  """Value: assistant"""
  assistant
}

type MessageAggregate {
  count: Int!
  min: MessageMinAggregate
  max: MessageMaxAggregate
  countNonNull: MessageCountNonNullAggregate
  countDistinct: MessageCountDistinctAggregate
}

type MessageMinAggregate {
  id: String
  taskId: String
  role: MessagesRoleEnum
  content: String
  createdAt: DateTime
}

type MessageMaxAggregate {
  id: String
  taskId: String
  role: MessagesRoleEnum
  content: String
  createdAt: DateTime
}

type MessageCountNonNullAggregate {
  id: Int!
  taskId: Int!
  role: Int!
  content: Int!
  createdAt: Int!
}

type MessageCountDistinctAggregate {
  id: Int!
  taskId: Int!
  role: Int!
  content: Int!
  createdAt: Int!
}

type MessageGroupBy {
  group: MessageGroupKeys!
  count: Int!
  min: MessageMinAggregate
  max: MessageMaxAggregate
  countNonNull: MessageCountNonNullAggregate
  countDistinct: MessageCountDistinctAggregate
}

"""
The grouped column values of one Message group. A column the query did not group by is null.
"""
type MessageGroupKeys {
  id: String
  taskId: String
  role: MessagesRoleEnum
  content: String
  createdAt: DateTime
}

type Card {
  id: String!
  projectId: String!
  taskId: String
  parentId: String
  laneId: String!
  title: String!
  body: String!
  acceptance: String!
  position: Int!
  status: CardsStatusEnum!
  error: String!
  attempts: Int!
  archivedAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  project(where: ProjectFilters): Project!
  lane(where: LaneFilters): Lane!
  task(where: TaskFilters): Task
  deps(
    where: CardDepFilters
    orderBy: CardDepOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [CardDepDistinctColumn!]
  ): [CardDep!]!
  depsAggregate(where: CardDepFilters): CardDepAggregate!
  blocks(
    where: CardDepFilters
    orderBy: CardDepOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [CardDepDistinctColumn!]
  ): [CardDep!]!
  blocksAggregate(where: CardDepFilters): CardDepAggregate!
  runs(
    where: RunFilters
    orderBy: RunOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [RunDistinctColumn!]
  ): [Run!]!
  runsAggregate(where: RunFilters): RunAggregate!
  events(
    where: CardEventFilters
    orderBy: CardEventOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [CardEventDistinctColumn!]
  ): [CardEvent!]!
  eventsAggregate(where: CardEventFilters): CardEventAggregate!
  notes(
    where: CardNoteFilters
    orderBy: CardNoteOrderBy
    offset: Int
    limit: Int

    """
    Keyset pagination: only return related rows strictly after this cursor (a row's `cursor` field from a previous page of this relation, under the same orderBy).
    """
    after: String
    distinct: [CardNoteDistinctColumn!]
  ): [CardNote!]!
  notesAggregate(where: CardNoteFilters): CardNoteAggregate!
}

enum CardsStatusEnum {
  """Value: idle"""
  idle

  """Value: running"""
  running

  """Value: done"""
  done

  """Value: rejected"""
  rejected

  """Value: error"""
  error
}

"""Columns of CardDep that a query can be made distinct on"""
enum CardDepDistinctColumn {
  id
  cardId
  dependsOnCardId
}

"""Columns of CardEvent that a query can be made distinct on"""
enum CardEventDistinctColumn {
  id
  cardId
  runId
  fromLaneId
  toLaneId
  noteId
  actor
  createdAt
}

"""Columns of CardNote that a query can be made distinct on"""
enum CardNoteDistinctColumn {
  id
  cardId
  runId
  kind
  author
  body
  createdAt
  updatedAt
}

type CardAggregate {
  count: Int!
  avg: CardAvgAggregate
  sum: CardSumAggregate
  min: CardMinAggregate
  max: CardMaxAggregate
  countNonNull: CardCountNonNullAggregate
  countDistinct: CardCountDistinctAggregate
}

type CardAvgAggregate {
  position: Float
  attempts: Float
}

type CardSumAggregate {
  position: Float
  attempts: Float
}

type CardMinAggregate {
  id: String
  projectId: String
  taskId: String
  parentId: String
  laneId: String
  title: String
  body: String
  acceptance: String
  position: Int
  status: CardsStatusEnum
  error: String
  attempts: Int
  archivedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

type CardMaxAggregate {
  id: String
  projectId: String
  taskId: String
  parentId: String
  laneId: String
  title: String
  body: String
  acceptance: String
  position: Int
  status: CardsStatusEnum
  error: String
  attempts: Int
  archivedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

type CardCountNonNullAggregate {
  id: Int!
  projectId: Int!
  taskId: Int!
  parentId: Int!
  laneId: Int!
  title: Int!
  body: Int!
  acceptance: Int!
  position: Int!
  status: Int!
  error: Int!
  attempts: Int!
  archivedAt: Int!
  createdAt: Int!
  updatedAt: Int!
}

type CardCountDistinctAggregate {
  id: Int!
  projectId: Int!
  taskId: Int!
  parentId: Int!
  laneId: Int!
  title: Int!
  body: Int!
  acceptance: Int!
  position: Int!
  status: Int!
  error: Int!
  attempts: Int!
  archivedAt: Int!
  createdAt: Int!
  updatedAt: Int!
}

type CardGroupBy {
  group: CardGroupKeys!
  count: Int!
  avg: CardAvgAggregate
  sum: CardSumAggregate
  min: CardMinAggregate
  max: CardMaxAggregate
  countNonNull: CardCountNonNullAggregate
  countDistinct: CardCountDistinctAggregate
}

"""
The grouped column values of one Card group. A column the query did not group by is null.
"""
type CardGroupKeys {
  id: String
  projectId: String
  taskId: String
  parentId: String
  laneId: String
  title: String
  body: String
  acceptance: String
  position: Int
  status: CardsStatusEnum
  error: String
  attempts: Int
  archivedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

type CardDep {
  id: String!
  cardId: String!
  dependsOnCardId: String!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  card(where: CardFilters): Card!
  dependsOn(where: CardFilters): Card!
}

type CardDepAggregate {
  count: Int!
  min: CardDepMinAggregate
  max: CardDepMaxAggregate
  countNonNull: CardDepCountNonNullAggregate
  countDistinct: CardDepCountDistinctAggregate
}

type CardDepMinAggregate {
  id: String
  cardId: String
  dependsOnCardId: String
}

type CardDepMaxAggregate {
  id: String
  cardId: String
  dependsOnCardId: String
}

type CardDepCountNonNullAggregate {
  id: Int!
  cardId: Int!
  dependsOnCardId: Int!
}

type CardDepCountDistinctAggregate {
  id: Int!
  cardId: Int!
  dependsOnCardId: Int!
}

type CardDepGroupBy {
  group: CardDepGroupKeys!
  count: Int!
  min: CardDepMinAggregate
  max: CardDepMaxAggregate
  countNonNull: CardDepCountNonNullAggregate
  countDistinct: CardDepCountDistinctAggregate
}

"""
The grouped column values of one CardDep group. A column the query did not group by is null.
"""
type CardDepGroupKeys {
  id: String
  cardId: String
  dependsOnCardId: String
}

type CardNote {
  id: String!
  cardId: String!
  runId: String
  kind: CardNotesKindEnum!
  author: CardNotesAuthorEnum!
  body: String!
  createdAt: DateTime!
  updatedAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  card(where: CardFilters): Card!
  run(where: RunFilters): Run
}

enum CardNotesKindEnum {
  """Value: report"""
  report

  """Value: verdict"""
  verdict

  """Value: note"""
  note
}

enum CardNotesAuthorEnum {
  """Value: agent"""
  agent

  """Value: user"""
  user
}

type CardNoteAggregate {
  count: Int!
  min: CardNoteMinAggregate
  max: CardNoteMaxAggregate
  countNonNull: CardNoteCountNonNullAggregate
  countDistinct: CardNoteCountDistinctAggregate
}

type CardNoteMinAggregate {
  id: String
  cardId: String
  runId: String
  kind: CardNotesKindEnum
  author: CardNotesAuthorEnum
  body: String
  createdAt: DateTime
  updatedAt: DateTime
}

type CardNoteMaxAggregate {
  id: String
  cardId: String
  runId: String
  kind: CardNotesKindEnum
  author: CardNotesAuthorEnum
  body: String
  createdAt: DateTime
  updatedAt: DateTime
}

type CardNoteCountNonNullAggregate {
  id: Int!
  cardId: Int!
  runId: Int!
  kind: Int!
  author: Int!
  body: Int!
  createdAt: Int!
  updatedAt: Int!
}

type CardNoteCountDistinctAggregate {
  id: Int!
  cardId: Int!
  runId: Int!
  kind: Int!
  author: Int!
  body: Int!
  createdAt: Int!
  updatedAt: Int!
}

type CardNoteGroupBy {
  group: CardNoteGroupKeys!
  count: Int!
  min: CardNoteMinAggregate
  max: CardNoteMaxAggregate
  countNonNull: CardNoteCountNonNullAggregate
  countDistinct: CardNoteCountDistinctAggregate
}

"""
The grouped column values of one CardNote group. A column the query did not group by is null.
"""
type CardNoteGroupKeys {
  id: String
  cardId: String
  runId: String
  kind: CardNotesKindEnum
  author: CardNotesAuthorEnum
  body: String
  createdAt: DateTime
  updatedAt: DateTime
}

type CardEvent {
  id: String!
  cardId: String!
  runId: String
  fromLaneId: String
  toLaneId: String
  noteId: String
  actor: CardEventsActorEnum!
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  card(where: CardFilters): Card!
  run(where: RunFilters): Run
  note(where: CardNoteFilters): CardNote
  fromLane(where: LaneFilters): Lane
  toLane(where: LaneFilters): Lane
}

enum CardEventsActorEnum {
  """Value: agent"""
  agent

  """Value: user"""
  user

  """Value: system"""
  system
}

type CardEventAggregate {
  count: Int!
  min: CardEventMinAggregate
  max: CardEventMaxAggregate
  countNonNull: CardEventCountNonNullAggregate
  countDistinct: CardEventCountDistinctAggregate
}

type CardEventMinAggregate {
  id: String
  cardId: String
  runId: String
  fromLaneId: String
  toLaneId: String
  noteId: String
  actor: CardEventsActorEnum
  createdAt: DateTime
}

type CardEventMaxAggregate {
  id: String
  cardId: String
  runId: String
  fromLaneId: String
  toLaneId: String
  noteId: String
  actor: CardEventsActorEnum
  createdAt: DateTime
}

type CardEventCountNonNullAggregate {
  id: Int!
  cardId: Int!
  runId: Int!
  fromLaneId: Int!
  toLaneId: Int!
  noteId: Int!
  actor: Int!
  createdAt: Int!
}

type CardEventCountDistinctAggregate {
  id: Int!
  cardId: Int!
  runId: Int!
  fromLaneId: Int!
  toLaneId: Int!
  noteId: Int!
  actor: Int!
  createdAt: Int!
}

type CardEventGroupBy {
  group: CardEventGroupKeys!
  count: Int!
  min: CardEventMinAggregate
  max: CardEventMaxAggregate
  countNonNull: CardEventCountNonNullAggregate
  countDistinct: CardEventCountDistinctAggregate
}

"""
The grouped column values of one CardEvent group. A column the query did not group by is null.
"""
type CardEventGroupKeys {
  id: String
  cardId: String
  runId: String
  fromLaneId: String
  toLaneId: String
  noteId: String
  actor: CardEventsActorEnum
  createdAt: DateTime
}

type Run {
  id: String!
  projectId: String!
  agentId: String
  kind: RunsKindEnum!
  taskId: String
  cardId: String
  laneId: String
  status: RunsStatusEnum!
  verdict: RunsVerdictEnum!
  startedAt: DateTime!
  finishedAt: DateTime
  output: String!
  error: String!
  toolCalls: JSON
  promptTokens: Int!
  completionTokens: Int!
  totalTokens: Int!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
  project(where: ProjectFilters): Project!
  agent(where: AgentFilters): Agent
  task(where: TaskFilters): Task
  card(where: CardFilters): Card
  lane(where: LaneFilters): Lane
}

enum RunsKindEnum {
  """Value: refine"""
  refine

  """Value: decompose"""
  decompose

  """Value: card"""
  card
}

enum RunsStatusEnum {
  """Value: running"""
  running

  """Value: ok"""
  ok

  """Value: error"""
  error

  """Value: stopped"""
  stopped
}

enum RunsVerdictEnum {
  """Value: none"""
  none

  """Value: pass"""
  pass

  """Value: fail"""
  fail
}

type RunAggregate {
  count: Int!
  avg: RunAvgAggregate
  sum: RunSumAggregate
  min: RunMinAggregate
  max: RunMaxAggregate
  countNonNull: RunCountNonNullAggregate
  countDistinct: RunCountDistinctAggregate
}

type RunAvgAggregate {
  promptTokens: Float
  completionTokens: Float
  totalTokens: Float
}

type RunSumAggregate {
  promptTokens: Float
  completionTokens: Float
  totalTokens: Float
}

type RunMinAggregate {
  id: String
  projectId: String
  agentId: String
  kind: RunsKindEnum
  taskId: String
  cardId: String
  laneId: String
  status: RunsStatusEnum
  verdict: RunsVerdictEnum
  startedAt: DateTime
  finishedAt: DateTime
  output: String
  error: String
  promptTokens: Int
  completionTokens: Int
  totalTokens: Int
}

type RunMaxAggregate {
  id: String
  projectId: String
  agentId: String
  kind: RunsKindEnum
  taskId: String
  cardId: String
  laneId: String
  status: RunsStatusEnum
  verdict: RunsVerdictEnum
  startedAt: DateTime
  finishedAt: DateTime
  output: String
  error: String
  promptTokens: Int
  completionTokens: Int
  totalTokens: Int
}

type RunCountNonNullAggregate {
  id: Int!
  projectId: Int!
  agentId: Int!
  kind: Int!
  taskId: Int!
  cardId: Int!
  laneId: Int!
  status: Int!
  verdict: Int!
  startedAt: Int!
  finishedAt: Int!
  output: Int!
  error: Int!
  toolCalls: Int!
  promptTokens: Int!
  completionTokens: Int!
  totalTokens: Int!
}

type RunCountDistinctAggregate {
  id: Int!
  projectId: Int!
  agentId: Int!
  kind: Int!
  taskId: Int!
  cardId: Int!
  laneId: Int!
  status: Int!
  verdict: Int!
  startedAt: Int!
  finishedAt: Int!
  output: Int!
  error: Int!
  promptTokens: Int!
  completionTokens: Int!
  totalTokens: Int!
}

type RunGroupBy {
  group: RunGroupKeys!
  count: Int!
  avg: RunAvgAggregate
  sum: RunSumAggregate
  min: RunMinAggregate
  max: RunMaxAggregate
  countNonNull: RunCountNonNullAggregate
  countDistinct: RunCountDistinctAggregate
}

"""
The grouped column values of one Run group. A column the query did not group by is null.
"""
type RunGroupKeys {
  id: String
  projectId: String
  agentId: String
  kind: RunsKindEnum
  taskId: String
  cardId: String
  laneId: String
  status: RunsStatusEnum
  verdict: RunsVerdictEnum
  startedAt: DateTime
  finishedAt: DateTime
  output: String
  error: String
  promptTokens: Int
  completionTokens: Int
  totalTokens: Int
}

type BoardTemplate {
  id: String!
  name: String!
  description: String!
  lanes: JSON!
  createdAt: DateTime!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
}

type BoardTemplateAggregate {
  count: Int!
  min: BoardTemplateMinAggregate
  max: BoardTemplateMaxAggregate
  countNonNull: BoardTemplateCountNonNullAggregate
  countDistinct: BoardTemplateCountDistinctAggregate
}

type BoardTemplateMinAggregate {
  id: String
  name: String
  description: String
  createdAt: DateTime
}

type BoardTemplateMaxAggregate {
  id: String
  name: String
  description: String
  createdAt: DateTime
}

type BoardTemplateCountNonNullAggregate {
  id: Int!
  name: Int!
  description: Int!
  lanes: Int!
  createdAt: Int!
}

type BoardTemplateCountDistinctAggregate {
  id: Int!
  name: Int!
  description: Int!
  createdAt: Int!
}

type BoardTemplateGroupBy {
  group: BoardTemplateGroupKeys!
  count: Int!
  min: BoardTemplateMinAggregate
  max: BoardTemplateMaxAggregate
  countNonNull: BoardTemplateCountNonNullAggregate
  countDistinct: BoardTemplateCountDistinctAggregate
}

"""
The grouped column values of one BoardTemplate group. A column the query did not group by is null.
"""
type BoardTemplateGroupKeys {
  id: String
  name: String
  description: String
  createdAt: DateTime
}

type Setting {
  id: String!
  baseUrl: String!
  model: String!
  maxTokens: Int!
  temperature: Float!
  maxToolIterations: Int!
  toolDiscovery: SettingsToolDiscoveryEnum!
  toolSelectModel: String!
  requestTimeoutSeconds: Int!
  maxRetries: Int!
  runRetentionDays: Int!
  workerIntervalSeconds: Int!
  refineAgentId: String
  refinePrompt: String!

  """
  Opaque cursor of this row's position in the query's ordering. Pass it as `after` to resume from here. Only set on rows returned by a list query.
  """
  cursor: String
}

enum SettingsToolDiscoveryEnum {
  """Value: eager"""
  eager

  """Value: ondemand"""
  ondemand
}

type SettingAggregate {
  count: Int!
  avg: SettingAvgAggregate
  sum: SettingSumAggregate
  min: SettingMinAggregate
  max: SettingMaxAggregate
  countNonNull: SettingCountNonNullAggregate
  countDistinct: SettingCountDistinctAggregate
}

type SettingAvgAggregate {
  maxTokens: Float
  temperature: Float
  maxToolIterations: Float
  requestTimeoutSeconds: Float
  maxRetries: Float
  runRetentionDays: Float
  workerIntervalSeconds: Float
}

type SettingSumAggregate {
  maxTokens: Float
  temperature: Float
  maxToolIterations: Float
  requestTimeoutSeconds: Float
  maxRetries: Float
  runRetentionDays: Float
  workerIntervalSeconds: Float
}

type SettingMinAggregate {
  id: String
  baseUrl: String
  model: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: SettingsToolDiscoveryEnum
  toolSelectModel: String
  requestTimeoutSeconds: Int
  maxRetries: Int
  runRetentionDays: Int
  workerIntervalSeconds: Int
  refineAgentId: String
  refinePrompt: String
}

type SettingMaxAggregate {
  id: String
  baseUrl: String
  model: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: SettingsToolDiscoveryEnum
  toolSelectModel: String
  requestTimeoutSeconds: Int
  maxRetries: Int
  runRetentionDays: Int
  workerIntervalSeconds: Int
  refineAgentId: String
  refinePrompt: String
}

type SettingCountNonNullAggregate {
  id: Int!
  baseUrl: Int!
  model: Int!
  maxTokens: Int!
  temperature: Int!
  maxToolIterations: Int!
  toolDiscovery: Int!
  toolSelectModel: Int!
  requestTimeoutSeconds: Int!
  maxRetries: Int!
  runRetentionDays: Int!
  workerIntervalSeconds: Int!
  refineAgentId: Int!
  refinePrompt: Int!
}

type SettingCountDistinctAggregate {
  id: Int!
  baseUrl: Int!
  model: Int!
  maxTokens: Int!
  temperature: Int!
  maxToolIterations: Int!
  toolDiscovery: Int!
  toolSelectModel: Int!
  requestTimeoutSeconds: Int!
  maxRetries: Int!
  runRetentionDays: Int!
  workerIntervalSeconds: Int!
  refineAgentId: Int!
  refinePrompt: Int!
}

type SettingGroupBy {
  group: SettingGroupKeys!
  count: Int!
  avg: SettingAvgAggregate
  sum: SettingSumAggregate
  min: SettingMinAggregate
  max: SettingMaxAggregate
  countNonNull: SettingCountNonNullAggregate
  countDistinct: SettingCountDistinctAggregate
}

"""
The grouped column values of one Setting group. A column the query did not group by is null.
"""
type SettingGroupKeys {
  id: String
  baseUrl: String
  model: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: SettingsToolDiscoveryEnum
  toolSelectModel: String
  requestTimeoutSeconds: Int
  maxRetries: Int
  runRetentionDays: Int
  workerIntervalSeconds: Int
  refineAgentId: String
  refinePrompt: String
}

input CreateRoleInput {
  id: String
  name: String!
  description: String
  contract: RolesContractEnum
  prompt: String
  createdAt: DateTime
  updatedAt: DateTime
}

input UpdateRoleInput {
  id: String
  name: String
  description: String
  contract: RolesContractEnum
  prompt: String
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One entry of a batch update of Role: the rows `where` matches get this entry's `set` applied.
"""
input UpdateRoleManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: RoleFilters
  set: UpdateRoleInput!
}

input RoleFilters {
  id: StringFilter
  name: StringFilter
  description: StringFilter
  contract: RolesContractEnumFilter
  prompt: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  lanes: LaneListRelationFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [RoleFilters!]

  """Every branch matches"""
  AND: [RoleFilters!]

  """Negates the nested filters"""
  NOT: RoleFilters
}

input StringFilter {
  """Equal to"""
  eq: String

  """Not equal to"""
  ne: String

  """Less than"""
  lt: String

  """Less than or equal to"""
  lte: String

  """Greater than"""
  gt: String

  """Greater than or equal to"""
  gte: String
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [String!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [String!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [StringFilter!]

  """Every branch matches"""
  AND: [StringFilter!]

  """Negates the nested operators"""
  NOT: StringFilter
}

input RolesContractEnumFilter {
  """Equal to"""
  eq: RolesContractEnum

  """Not equal to"""
  ne: RolesContractEnum

  """Less than"""
  lt: RolesContractEnum

  """Less than or equal to"""
  lte: RolesContractEnum

  """Greater than"""
  gt: RolesContractEnum

  """Greater than or equal to"""
  gte: RolesContractEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [RolesContractEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [RolesContractEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [RolesContractEnumFilter!]

  """Every branch matches"""
  AND: [RolesContractEnumFilter!]

  """Negates the nested operators"""
  NOT: RolesContractEnumFilter
}

input DateTimeFilter {
  """Equal to"""
  eq: DateTime

  """Not equal to"""
  ne: DateTime

  """Less than"""
  lt: DateTime

  """Less than or equal to"""
  lte: DateTime

  """Greater than"""
  gt: DateTime

  """Greater than or equal to"""
  gte: DateTime
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [DateTime!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [DateTime!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [DateTimeFilter!]

  """Every branch matches"""
  AND: [DateTimeFilter!]

  """Negates the nested operators"""
  NOT: DateTimeFilter
}

input LaneListRelationFilter {
  """At least one related row matches"""
  some: LaneFilters

  """No related row matches"""
  none: LaneFilters

  """Every related row matches"""
  every: LaneFilters
}

input RoleOrderBy {
  id: InnerOrder
  name: InnerOrder
  description: InnerOrder
  contract: InnerOrder
  prompt: InnerOrder
  createdAt: InnerOrder
  updatedAt: InnerOrder
}

input InnerOrder {
  direction: OrderDirection!

  """Priority of current field"""
  priority: Int!

  """
  Where NULL values sort. Defaults to the database's own rule (PostgreSQL: last on asc, first on desc; MySQL/SQLite: first on asc, last on desc)
  """
  nulls: OrderNulls

  """
  Sort by this column's position in the `inArray` list the same request's `where` gives it, rather than by the column's own value — `direction: asc` keeps the list's order, `desc` reverses it. Requires an `inArray` filter on the same column at the top level of `where`, and cannot be combined with `after` or `distinct`.
  """
  matchFilterOrder: Boolean
}

"""Order by direction"""
enum OrderDirection {
  """Ascending order"""
  asc

  """Descending order"""
  desc
}

"""Where NULL values sort relative to non-NULL values"""
enum OrderNulls {
  """NULL values sort before all non-NULL values"""
  first

  """NULL values sort after all non-NULL values"""
  last
}

"""Filters Role groups by their aggregated values"""
input RoleHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: RoleCountNonNullHaving
  countDistinct: RoleCountDistinctHaving
}

"""
Compares an aggregated value. Several operators in one filter are ANDed together.
"""
input AggregateNumberFilter {
  eq: Float
  ne: Float
  gt: Float
  gte: Float
  lt: Float
  lte: Float
}

input RoleCountNonNullHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  description: AggregateNumberFilter
  contract: AggregateNumberFilter
  prompt: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input RoleCountDistinctHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  description: AggregateNumberFilter
  contract: AggregateNumberFilter
  prompt: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CreateAgentInput {
  id: String
  name: String!
  enabled: Boolean
  baseUrl: String
  model: String
  systemPrompt: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: AgentsToolDiscoveryEnum
  requestTimeoutSeconds: Int
  maxRetries: Int
  createdAt: DateTime
  updatedAt: DateTime
}

input UpdateAgentInput {
  id: String
  name: String
  enabled: Boolean
  baseUrl: String
  model: String
  systemPrompt: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: AgentsToolDiscoveryEnum
  requestTimeoutSeconds: Int
  maxRetries: Int
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One entry of a batch update of Agent: the rows `where` matches get this entry's `set` applied.
"""
input UpdateAgentManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: AgentFilters
  set: UpdateAgentInput!
}

input AgentFilters {
  id: StringFilter
  name: StringFilter
  enabled: BooleanFilter
  baseUrl: StringFilter
  model: StringFilter
  systemPrompt: StringFilter
  maxTokens: IntFilter
  temperature: FloatFilter
  maxToolIterations: IntFilter
  toolDiscovery: AgentsToolDiscoveryEnumFilter
  requestTimeoutSeconds: IntFilter
  maxRetries: IntFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  servers: AgentServerListRelationFilter
  lanes: LaneListRelationFilter
  runs: RunListRelationFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [AgentFilters!]

  """Every branch matches"""
  AND: [AgentFilters!]

  """Negates the nested filters"""
  NOT: AgentFilters
}

input BooleanFilter {
  """Equal to"""
  eq: Boolean

  """Not equal to"""
  ne: Boolean

  """Less than"""
  lt: Boolean

  """Less than or equal to"""
  lte: Boolean

  """Greater than"""
  gt: Boolean

  """Greater than or equal to"""
  gte: Boolean
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [Boolean!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [Boolean!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [BooleanFilter!]

  """Every branch matches"""
  AND: [BooleanFilter!]

  """Negates the nested operators"""
  NOT: BooleanFilter
}

input IntFilter {
  """Equal to"""
  eq: Int

  """Not equal to"""
  ne: Int

  """Less than"""
  lt: Int

  """Less than or equal to"""
  lte: Int

  """Greater than"""
  gt: Int

  """Greater than or equal to"""
  gte: Int

  """Matches any one of these values (SQL `IN`)"""
  inArray: [Int!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [Int!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [IntFilter!]

  """Every branch matches"""
  AND: [IntFilter!]

  """Negates the nested operators"""
  NOT: IntFilter
}

input FloatFilter {
  """Equal to"""
  eq: Float

  """Not equal to"""
  ne: Float

  """Less than"""
  lt: Float

  """Less than or equal to"""
  lte: Float

  """Greater than"""
  gt: Float

  """Greater than or equal to"""
  gte: Float

  """Matches any one of these values (SQL `IN`)"""
  inArray: [Float!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [Float!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [FloatFilter!]

  """Every branch matches"""
  AND: [FloatFilter!]

  """Negates the nested operators"""
  NOT: FloatFilter
}

input AgentsToolDiscoveryEnumFilter {
  """Equal to"""
  eq: AgentsToolDiscoveryEnum

  """Not equal to"""
  ne: AgentsToolDiscoveryEnum

  """Less than"""
  lt: AgentsToolDiscoveryEnum

  """Less than or equal to"""
  lte: AgentsToolDiscoveryEnum

  """Greater than"""
  gt: AgentsToolDiscoveryEnum

  """Greater than or equal to"""
  gte: AgentsToolDiscoveryEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [AgentsToolDiscoveryEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [AgentsToolDiscoveryEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [AgentsToolDiscoveryEnumFilter!]

  """Every branch matches"""
  AND: [AgentsToolDiscoveryEnumFilter!]

  """Negates the nested operators"""
  NOT: AgentsToolDiscoveryEnumFilter
}

input AgentServerListRelationFilter {
  """At least one related row matches"""
  some: AgentServerFilters

  """No related row matches"""
  none: AgentServerFilters

  """Every related row matches"""
  every: AgentServerFilters
}

input RunListRelationFilter {
  """At least one related row matches"""
  some: RunFilters

  """No related row matches"""
  none: RunFilters

  """Every related row matches"""
  every: RunFilters
}

input AgentOrderBy {
  id: InnerOrder
  name: InnerOrder
  enabled: InnerOrder
  baseUrl: InnerOrder
  model: InnerOrder
  systemPrompt: InnerOrder
  maxTokens: InnerOrder
  temperature: InnerOrder
  maxToolIterations: InnerOrder
  toolDiscovery: InnerOrder
  requestTimeoutSeconds: InnerOrder
  maxRetries: InnerOrder
  createdAt: InnerOrder
  updatedAt: InnerOrder
}

"""Filters Agent groups by their aggregated values"""
input AgentHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  avg: AgentAvgHaving
  sum: AgentSumHaving
  min: AgentMinHaving
  max: AgentMaxHaving
  countNonNull: AgentCountNonNullHaving
  countDistinct: AgentCountDistinctHaving
}

input AgentAvgHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
}

input AgentSumHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
}

input AgentMinHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
}

input AgentMaxHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
}

input AgentCountNonNullHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  enabled: AggregateNumberFilter
  baseUrl: AggregateNumberFilter
  model: AggregateNumberFilter
  systemPrompt: AggregateNumberFilter
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  toolDiscovery: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input AgentCountDistinctHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  baseUrl: AggregateNumberFilter
  model: AggregateNumberFilter
  systemPrompt: AggregateNumberFilter
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  toolDiscovery: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CreateMcpServerInput {
  id: String
  slug: String!
  label: String
  enabled: Boolean
  transport: McpServersTransportEnum
  command: String
  args: JSON
  env: JSON
  url: String
  headers: JSON
}

input UpdateMcpServerInput {
  id: String
  slug: String
  label: String
  enabled: Boolean
  transport: McpServersTransportEnum
  command: String
  args: JSON
  env: JSON
  url: String
  headers: JSON
}

"""
One entry of a batch update of McpServer: the rows `where` matches get this entry's `set` applied.
"""
input UpdateMcpServerManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: McpServerFilters
  set: UpdateMcpServerInput!
}

input McpServerFilters {
  id: StringFilter
  slug: StringFilter
  label: StringFilter
  enabled: BooleanFilter
  transport: McpServersTransportEnumFilter
  command: StringFilter
  args: JSONFilter
  env: JSONFilter
  url: StringFilter
  headers: JSONFilter
  agents: AgentServerListRelationFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [McpServerFilters!]

  """Every branch matches"""
  AND: [McpServerFilters!]

  """Negates the nested filters"""
  NOT: McpServerFilters
}

input McpServersTransportEnumFilter {
  """Equal to"""
  eq: McpServersTransportEnum

  """Not equal to"""
  ne: McpServersTransportEnum

  """Less than"""
  lt: McpServersTransportEnum

  """Less than or equal to"""
  lte: McpServersTransportEnum

  """Greater than"""
  gt: McpServersTransportEnum

  """Greater than or equal to"""
  gte: McpServersTransportEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [McpServersTransportEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [McpServersTransportEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [McpServersTransportEnumFilter!]

  """Every branch matches"""
  AND: [McpServersTransportEnumFilter!]

  """Negates the nested operators"""
  NOT: McpServersTransportEnumFilter
}

input JSONFilter {
  """JSON equality on the whole value"""
  eq: JSON

  """JSON inequality on the whole value"""
  ne: JSON

  """
  Value structurally contains this JSON (Postgres `@>` / MySQL JSON_CONTAINS)
  """
  contains: JSON

  """
  Compares the value at one path inside the document. Several entries are ANDed; a single object may be passed without the list brackets.
  """
  path: [JSONPathFilter!]
  isNull: Boolean
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [JSONFilter!]

  """Every branch matches"""
  AND: [JSONFilter!]

  """Negates the nested operators"""
  NOT: JSONFilter
}

input JSONPathFilter {
  """
  Keys to walk from the document root, e.g. `["profile", "level"]`. An all-digits key indexes an array.
  """
  path: [String!]!

  """Overrides how the value is read before comparing"""
  as: JSONPathCast

  """Equal to"""
  eq: JSON

  """Not equal to"""
  ne: JSON

  """Less than"""
  lt: JSON

  """Less than or equal to"""
  lte: JSON

  """Greater than"""
  gt: JSON

  """Greater than or equal to"""
  gte: JSON

  """
  Extracted value starts with this string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Extracted value ends with this string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Extracted value contains this string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """When true, matches rows where the path is missing or holds JSON null"""
  isNull: Boolean

  """When true, matches rows where the path holds a value"""
  isNotNull: Boolean
}

"""How to read the value at a JSON path before comparing it"""
enum JSONPathCast {
  """Compare as text (lexicographic ordering)"""
  TEXT

  """Compare as a number; a non-numeric value never matches"""
  NUMBER

  """Compare as a boolean"""
  BOOLEAN
}

input McpServerOrderBy {
  id: InnerOrder
  slug: InnerOrder
  label: InnerOrder
  enabled: InnerOrder
  transport: InnerOrder
  command: InnerOrder
  args: InnerOrder
  env: InnerOrder
  url: InnerOrder
  headers: InnerOrder
}

"""Filters McpServer groups by their aggregated values"""
input McpServerHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: McpServerCountNonNullHaving
  countDistinct: McpServerCountDistinctHaving
}

input McpServerCountNonNullHaving {
  id: AggregateNumberFilter
  slug: AggregateNumberFilter
  label: AggregateNumberFilter
  enabled: AggregateNumberFilter
  transport: AggregateNumberFilter
  command: AggregateNumberFilter
  args: AggregateNumberFilter
  env: AggregateNumberFilter
  url: AggregateNumberFilter
  headers: AggregateNumberFilter
}

input McpServerCountDistinctHaving {
  id: AggregateNumberFilter
  slug: AggregateNumberFilter
  label: AggregateNumberFilter
  transport: AggregateNumberFilter
  command: AggregateNumberFilter
  url: AggregateNumberFilter
}

input CreateAgentServerInput {
  id: String
  agentId: String!
  serverId: String!
}

input UpdateAgentServerInput {
  id: String
  agentId: String
  serverId: String
}

"""
One entry of a batch update of AgentServer: the rows `where` matches get this entry's `set` applied.
"""
input UpdateAgentServerManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: AgentServerFilters
  set: UpdateAgentServerInput!
}

input AgentServerFilters {
  id: StringFilter
  agentId: StringFilter
  serverId: StringFilter

  """Matches rows whose agent matches these filters"""
  agent: AgentFilters

  """Matches rows whose server matches these filters"""
  server: McpServerFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [AgentServerFilters!]

  """Every branch matches"""
  AND: [AgentServerFilters!]

  """Negates the nested filters"""
  NOT: AgentServerFilters
}

input AgentServerOrderBy {
  id: InnerOrder
  agentId: InnerOrder
  serverId: InnerOrder

  """Order by columns of the related agent row"""
  agent: AgentOrderBy

  """Order by columns of the related server row"""
  server: McpServerOrderBy
}

"""Filters AgentServer groups by their aggregated values"""
input AgentServerHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: AgentServerCountNonNullHaving
  countDistinct: AgentServerCountDistinctHaving
}

input AgentServerCountNonNullHaving {
  id: AggregateNumberFilter
  agentId: AggregateNumberFilter
  serverId: AggregateNumberFilter
}

input AgentServerCountDistinctHaving {
  id: AggregateNumberFilter
  agentId: AggregateNumberFilter
  serverId: AggregateNumberFilter
}

input CreateProjectInput {
  id: String
  name: String!
  description: String
  context: String
  autoRun: Boolean
  refineAgentId: String
  createdAt: DateTime
  updatedAt: DateTime
}

input UpdateProjectInput {
  id: String
  name: String
  description: String
  context: String
  autoRun: Boolean
  refineAgentId: String
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One entry of a batch update of Project: the rows `where` matches get this entry's `set` applied.
"""
input UpdateProjectManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: ProjectFilters
  set: UpdateProjectInput!
}

input ProjectFilters {
  id: StringFilter
  name: StringFilter
  description: StringFilter
  context: StringFilter
  autoRun: BooleanFilter
  refineAgentId: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  lanes: LaneListRelationFilter
  tasks: TaskListRelationFilter
  cards: CardListRelationFilter
  runs: RunListRelationFilter

  """Matches rows whose refineAgent matches these filters"""
  refineAgent: AgentFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [ProjectFilters!]

  """Every branch matches"""
  AND: [ProjectFilters!]

  """Negates the nested filters"""
  NOT: ProjectFilters
}

input TaskListRelationFilter {
  """At least one related row matches"""
  some: TaskFilters

  """No related row matches"""
  none: TaskFilters

  """Every related row matches"""
  every: TaskFilters
}

input CardListRelationFilter {
  """At least one related row matches"""
  some: CardFilters

  """No related row matches"""
  none: CardFilters

  """Every related row matches"""
  every: CardFilters
}

input ProjectOrderBy {
  id: InnerOrder
  name: InnerOrder
  description: InnerOrder
  context: InnerOrder
  autoRun: InnerOrder
  refineAgentId: InnerOrder
  createdAt: InnerOrder
  updatedAt: InnerOrder

  """Order by columns of the related refineAgent row"""
  refineAgent: AgentOrderBy
}

"""Filters Project groups by their aggregated values"""
input ProjectHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: ProjectCountNonNullHaving
  countDistinct: ProjectCountDistinctHaving
}

input ProjectCountNonNullHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  description: AggregateNumberFilter
  context: AggregateNumberFilter
  autoRun: AggregateNumberFilter
  refineAgentId: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input ProjectCountDistinctHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  description: AggregateNumberFilter
  context: AggregateNumberFilter
  refineAgentId: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CreateLaneInput {
  id: String
  projectId: String!
  name: String!
  position: Int
  intake: Boolean
  roleId: String
  prompt: String
  agentId: String
  onSuccessLaneId: String
  archiveOnSuccess: Boolean
  onFailureLaneId: String
  wipLimit: Int
  maxAttempts: Int
  createdAt: DateTime
}

input UpdateLaneInput {
  id: String
  projectId: String
  name: String
  position: Int
  intake: Boolean
  roleId: String
  prompt: String
  agentId: String
  onSuccessLaneId: String
  archiveOnSuccess: Boolean
  onFailureLaneId: String
  wipLimit: Int
  maxAttempts: Int
  createdAt: DateTime
}

"""
One entry of a batch update of Lane: the rows `where` matches get this entry's `set` applied.
"""
input UpdateLaneManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: LaneFilters
  set: UpdateLaneInput!
}

input LaneFilters {
  id: StringFilter
  projectId: StringFilter
  name: StringFilter
  position: IntFilter
  intake: BooleanFilter
  roleId: StringFilter
  prompt: StringFilter
  agentId: StringFilter
  onSuccessLaneId: StringFilter
  archiveOnSuccess: BooleanFilter
  onFailureLaneId: StringFilter
  wipLimit: IntFilter
  maxAttempts: IntFilter
  createdAt: DateTimeFilter

  """Matches rows whose project matches these filters"""
  project: ProjectFilters

  """Matches rows whose role matches these filters"""
  role: RoleFilters

  """Matches rows whose agent matches these filters"""
  agent: AgentFilters
  cards: CardListRelationFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [LaneFilters!]

  """Every branch matches"""
  AND: [LaneFilters!]

  """Negates the nested filters"""
  NOT: LaneFilters
}

input LaneOrderBy {
  id: InnerOrder
  projectId: InnerOrder
  name: InnerOrder
  position: InnerOrder
  intake: InnerOrder
  roleId: InnerOrder
  prompt: InnerOrder
  agentId: InnerOrder
  onSuccessLaneId: InnerOrder
  archiveOnSuccess: InnerOrder
  onFailureLaneId: InnerOrder
  wipLimit: InnerOrder
  maxAttempts: InnerOrder
  createdAt: InnerOrder

  """Order by columns of the related project row"""
  project: ProjectOrderBy

  """Order by columns of the related role row"""
  role: RoleOrderBy

  """Order by columns of the related agent row"""
  agent: AgentOrderBy
}

"""Filters Lane groups by their aggregated values"""
input LaneHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  avg: LaneAvgHaving
  sum: LaneSumHaving
  min: LaneMinHaving
  max: LaneMaxHaving
  countNonNull: LaneCountNonNullHaving
  countDistinct: LaneCountDistinctHaving
}

input LaneAvgHaving {
  position: AggregateNumberFilter
  wipLimit: AggregateNumberFilter
  maxAttempts: AggregateNumberFilter
}

input LaneSumHaving {
  position: AggregateNumberFilter
  wipLimit: AggregateNumberFilter
  maxAttempts: AggregateNumberFilter
}

input LaneMinHaving {
  position: AggregateNumberFilter
  wipLimit: AggregateNumberFilter
  maxAttempts: AggregateNumberFilter
}

input LaneMaxHaving {
  position: AggregateNumberFilter
  wipLimit: AggregateNumberFilter
  maxAttempts: AggregateNumberFilter
}

input LaneCountNonNullHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  name: AggregateNumberFilter
  position: AggregateNumberFilter
  intake: AggregateNumberFilter
  roleId: AggregateNumberFilter
  prompt: AggregateNumberFilter
  agentId: AggregateNumberFilter
  onSuccessLaneId: AggregateNumberFilter
  archiveOnSuccess: AggregateNumberFilter
  onFailureLaneId: AggregateNumberFilter
  wipLimit: AggregateNumberFilter
  maxAttempts: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input LaneCountDistinctHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  name: AggregateNumberFilter
  position: AggregateNumberFilter
  roleId: AggregateNumberFilter
  prompt: AggregateNumberFilter
  agentId: AggregateNumberFilter
  onSuccessLaneId: AggregateNumberFilter
  onFailureLaneId: AggregateNumberFilter
  wipLimit: AggregateNumberFilter
  maxAttempts: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input CreateTaskInput {
  id: String
  projectId: String!
  title: String
  brief: String
  createdAt: DateTime
  updatedAt: DateTime
}

input UpdateTaskInput {
  id: String
  projectId: String
  title: String
  brief: String
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One entry of a batch update of Task: the rows `where` matches get this entry's `set` applied.
"""
input UpdateTaskManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: TaskFilters
  set: UpdateTaskInput!
}

input TaskFilters {
  id: StringFilter
  projectId: StringFilter
  title: StringFilter
  brief: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter

  """Matches rows whose project matches these filters"""
  project: ProjectFilters
  messages: MessageListRelationFilter
  cards: CardListRelationFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [TaskFilters!]

  """Every branch matches"""
  AND: [TaskFilters!]

  """Negates the nested filters"""
  NOT: TaskFilters
}

input MessageListRelationFilter {
  """At least one related row matches"""
  some: MessageFilters

  """No related row matches"""
  none: MessageFilters

  """Every related row matches"""
  every: MessageFilters
}

input TaskOrderBy {
  id: InnerOrder
  projectId: InnerOrder
  title: InnerOrder
  brief: InnerOrder
  createdAt: InnerOrder
  updatedAt: InnerOrder

  """Order by columns of the related project row"""
  project: ProjectOrderBy
}

"""Filters Task groups by their aggregated values"""
input TaskHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: TaskCountNonNullHaving
  countDistinct: TaskCountDistinctHaving
}

input TaskCountNonNullHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  title: AggregateNumberFilter
  brief: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input TaskCountDistinctHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  title: AggregateNumberFilter
  brief: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CreateMessageInput {
  id: String
  taskId: String!
  role: MessagesRoleEnum
  content: String
  createdAt: DateTime
}

input UpdateMessageInput {
  id: String
  taskId: String
  role: MessagesRoleEnum
  content: String
  createdAt: DateTime
}

"""
One entry of a batch update of Message: the rows `where` matches get this entry's `set` applied.
"""
input UpdateMessageManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: MessageFilters
  set: UpdateMessageInput!
}

input MessageFilters {
  id: StringFilter
  taskId: StringFilter
  role: MessagesRoleEnumFilter
  content: StringFilter
  createdAt: DateTimeFilter

  """Matches rows whose task matches these filters"""
  task: TaskFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [MessageFilters!]

  """Every branch matches"""
  AND: [MessageFilters!]

  """Negates the nested filters"""
  NOT: MessageFilters
}

input MessagesRoleEnumFilter {
  """Equal to"""
  eq: MessagesRoleEnum

  """Not equal to"""
  ne: MessagesRoleEnum

  """Less than"""
  lt: MessagesRoleEnum

  """Less than or equal to"""
  lte: MessagesRoleEnum

  """Greater than"""
  gt: MessagesRoleEnum

  """Greater than or equal to"""
  gte: MessagesRoleEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [MessagesRoleEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [MessagesRoleEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [MessagesRoleEnumFilter!]

  """Every branch matches"""
  AND: [MessagesRoleEnumFilter!]

  """Negates the nested operators"""
  NOT: MessagesRoleEnumFilter
}

input MessageOrderBy {
  id: InnerOrder
  taskId: InnerOrder
  role: InnerOrder
  content: InnerOrder
  createdAt: InnerOrder

  """Order by columns of the related task row"""
  task: TaskOrderBy
}

"""Filters Message groups by their aggregated values"""
input MessageHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: MessageCountNonNullHaving
  countDistinct: MessageCountDistinctHaving
}

input MessageCountNonNullHaving {
  id: AggregateNumberFilter
  taskId: AggregateNumberFilter
  role: AggregateNumberFilter
  content: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input MessageCountDistinctHaving {
  id: AggregateNumberFilter
  taskId: AggregateNumberFilter
  role: AggregateNumberFilter
  content: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input CreateCardInput {
  id: String
  projectId: String!
  taskId: String
  parentId: String
  laneId: String!
  title: String!
  body: String
  acceptance: String
  position: Int
  status: CardsStatusEnum
  error: String
  attempts: Int
  archivedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

input UpdateCardInput {
  id: String
  projectId: String
  taskId: String
  parentId: String
  laneId: String
  title: String
  body: String
  acceptance: String
  position: Int
  status: CardsStatusEnum
  error: String
  attempts: Int
  archivedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One entry of a batch update of Card: the rows `where` matches get this entry's `set` applied.
"""
input UpdateCardManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: CardFilters
  set: UpdateCardInput!
}

input CardFilters {
  id: StringFilter
  projectId: StringFilter
  taskId: StringFilter
  parentId: StringFilter
  laneId: StringFilter
  title: StringFilter
  body: StringFilter
  acceptance: StringFilter
  position: IntFilter
  status: CardsStatusEnumFilter
  error: StringFilter
  attempts: IntFilter
  archivedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter

  """Matches rows whose project matches these filters"""
  project: ProjectFilters

  """Matches rows whose lane matches these filters"""
  lane: LaneFilters

  """Matches rows whose task matches these filters"""
  task: TaskFilters
  deps: CardDepListRelationFilter
  blocks: CardDepListRelationFilter
  runs: RunListRelationFilter
  events: CardEventListRelationFilter
  notes: CardNoteListRelationFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [CardFilters!]

  """Every branch matches"""
  AND: [CardFilters!]

  """Negates the nested filters"""
  NOT: CardFilters
}

input CardsStatusEnumFilter {
  """Equal to"""
  eq: CardsStatusEnum

  """Not equal to"""
  ne: CardsStatusEnum

  """Less than"""
  lt: CardsStatusEnum

  """Less than or equal to"""
  lte: CardsStatusEnum

  """Greater than"""
  gt: CardsStatusEnum

  """Greater than or equal to"""
  gte: CardsStatusEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [CardsStatusEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [CardsStatusEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [CardsStatusEnumFilter!]

  """Every branch matches"""
  AND: [CardsStatusEnumFilter!]

  """Negates the nested operators"""
  NOT: CardsStatusEnumFilter
}

input CardDepListRelationFilter {
  """At least one related row matches"""
  some: CardDepFilters

  """No related row matches"""
  none: CardDepFilters

  """Every related row matches"""
  every: CardDepFilters
}

input CardEventListRelationFilter {
  """At least one related row matches"""
  some: CardEventFilters

  """No related row matches"""
  none: CardEventFilters

  """Every related row matches"""
  every: CardEventFilters
}

input CardNoteListRelationFilter {
  """At least one related row matches"""
  some: CardNoteFilters

  """No related row matches"""
  none: CardNoteFilters

  """Every related row matches"""
  every: CardNoteFilters
}

input CardOrderBy {
  id: InnerOrder
  projectId: InnerOrder
  taskId: InnerOrder
  parentId: InnerOrder
  laneId: InnerOrder
  title: InnerOrder
  body: InnerOrder
  acceptance: InnerOrder
  position: InnerOrder
  status: InnerOrder
  error: InnerOrder
  attempts: InnerOrder
  archivedAt: InnerOrder
  createdAt: InnerOrder
  updatedAt: InnerOrder

  """Order by columns of the related project row"""
  project: ProjectOrderBy

  """Order by columns of the related lane row"""
  lane: LaneOrderBy

  """Order by columns of the related task row"""
  task: TaskOrderBy
}

"""Filters Card groups by their aggregated values"""
input CardHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  avg: CardAvgHaving
  sum: CardSumHaving
  min: CardMinHaving
  max: CardMaxHaving
  countNonNull: CardCountNonNullHaving
  countDistinct: CardCountDistinctHaving
}

input CardAvgHaving {
  position: AggregateNumberFilter
  attempts: AggregateNumberFilter
}

input CardSumHaving {
  position: AggregateNumberFilter
  attempts: AggregateNumberFilter
}

input CardMinHaving {
  position: AggregateNumberFilter
  attempts: AggregateNumberFilter
}

input CardMaxHaving {
  position: AggregateNumberFilter
  attempts: AggregateNumberFilter
}

input CardCountNonNullHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  taskId: AggregateNumberFilter
  parentId: AggregateNumberFilter
  laneId: AggregateNumberFilter
  title: AggregateNumberFilter
  body: AggregateNumberFilter
  acceptance: AggregateNumberFilter
  position: AggregateNumberFilter
  status: AggregateNumberFilter
  error: AggregateNumberFilter
  attempts: AggregateNumberFilter
  archivedAt: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CardCountDistinctHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  taskId: AggregateNumberFilter
  parentId: AggregateNumberFilter
  laneId: AggregateNumberFilter
  title: AggregateNumberFilter
  body: AggregateNumberFilter
  acceptance: AggregateNumberFilter
  position: AggregateNumberFilter
  status: AggregateNumberFilter
  error: AggregateNumberFilter
  attempts: AggregateNumberFilter
  archivedAt: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CreateCardDepInput {
  id: String
  cardId: String!
  dependsOnCardId: String!
}

input UpdateCardDepInput {
  id: String
  cardId: String
  dependsOnCardId: String
}

"""
One entry of a batch update of CardDep: the rows `where` matches get this entry's `set` applied.
"""
input UpdateCardDepManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: CardDepFilters
  set: UpdateCardDepInput!
}

input CardDepFilters {
  id: StringFilter
  cardId: StringFilter
  dependsOnCardId: StringFilter

  """Matches rows whose card matches these filters"""
  card: CardFilters

  """Matches rows whose dependsOn matches these filters"""
  dependsOn: CardFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [CardDepFilters!]

  """Every branch matches"""
  AND: [CardDepFilters!]

  """Negates the nested filters"""
  NOT: CardDepFilters
}

input CardDepOrderBy {
  id: InnerOrder
  cardId: InnerOrder
  dependsOnCardId: InnerOrder

  """Order by columns of the related card row"""
  card: CardOrderBy

  """Order by columns of the related dependsOn row"""
  dependsOn: CardOrderBy
}

"""Filters CardDep groups by their aggregated values"""
input CardDepHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: CardDepCountNonNullHaving
  countDistinct: CardDepCountDistinctHaving
}

input CardDepCountNonNullHaving {
  id: AggregateNumberFilter
  cardId: AggregateNumberFilter
  dependsOnCardId: AggregateNumberFilter
}

input CardDepCountDistinctHaving {
  id: AggregateNumberFilter
  cardId: AggregateNumberFilter
  dependsOnCardId: AggregateNumberFilter
}

input CardNoteFilters {
  id: StringFilter
  cardId: StringFilter
  runId: StringFilter
  kind: CardNotesKindEnumFilter
  author: CardNotesAuthorEnumFilter
  body: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter

  """Matches rows whose card matches these filters"""
  card: CardFilters

  """Matches rows whose run matches these filters"""
  run: RunFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [CardNoteFilters!]

  """Every branch matches"""
  AND: [CardNoteFilters!]

  """Negates the nested filters"""
  NOT: CardNoteFilters
}

input CardNotesKindEnumFilter {
  """Equal to"""
  eq: CardNotesKindEnum

  """Not equal to"""
  ne: CardNotesKindEnum

  """Less than"""
  lt: CardNotesKindEnum

  """Less than or equal to"""
  lte: CardNotesKindEnum

  """Greater than"""
  gt: CardNotesKindEnum

  """Greater than or equal to"""
  gte: CardNotesKindEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [CardNotesKindEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [CardNotesKindEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [CardNotesKindEnumFilter!]

  """Every branch matches"""
  AND: [CardNotesKindEnumFilter!]

  """Negates the nested operators"""
  NOT: CardNotesKindEnumFilter
}

input CardNotesAuthorEnumFilter {
  """Equal to"""
  eq: CardNotesAuthorEnum

  """Not equal to"""
  ne: CardNotesAuthorEnum

  """Less than"""
  lt: CardNotesAuthorEnum

  """Less than or equal to"""
  lte: CardNotesAuthorEnum

  """Greater than"""
  gt: CardNotesAuthorEnum

  """Greater than or equal to"""
  gte: CardNotesAuthorEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [CardNotesAuthorEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [CardNotesAuthorEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [CardNotesAuthorEnumFilter!]

  """Every branch matches"""
  AND: [CardNotesAuthorEnumFilter!]

  """Negates the nested operators"""
  NOT: CardNotesAuthorEnumFilter
}

input CardNoteOrderBy {
  id: InnerOrder
  cardId: InnerOrder
  runId: InnerOrder
  kind: InnerOrder
  author: InnerOrder
  body: InnerOrder
  createdAt: InnerOrder
  updatedAt: InnerOrder

  """Order by columns of the related card row"""
  card: CardOrderBy

  """Order by columns of the related run row"""
  run: RunOrderBy
}

"""Filters CardNote groups by their aggregated values"""
input CardNoteHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: CardNoteCountNonNullHaving
  countDistinct: CardNoteCountDistinctHaving
}

input CardNoteCountNonNullHaving {
  id: AggregateNumberFilter
  cardId: AggregateNumberFilter
  runId: AggregateNumberFilter
  kind: AggregateNumberFilter
  author: AggregateNumberFilter
  body: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CardNoteCountDistinctHaving {
  id: AggregateNumberFilter
  cardId: AggregateNumberFilter
  runId: AggregateNumberFilter
  kind: AggregateNumberFilter
  author: AggregateNumberFilter
  body: AggregateNumberFilter
  createdAt: AggregateNumberFilter
  updatedAt: AggregateNumberFilter
}

input CardEventFilters {
  id: StringFilter
  cardId: StringFilter
  runId: StringFilter
  fromLaneId: StringFilter
  toLaneId: StringFilter
  noteId: StringFilter
  actor: CardEventsActorEnumFilter
  createdAt: DateTimeFilter

  """Matches rows whose card matches these filters"""
  card: CardFilters

  """Matches rows whose run matches these filters"""
  run: RunFilters

  """Matches rows whose note matches these filters"""
  note: CardNoteFilters

  """Matches rows whose fromLane matches these filters"""
  fromLane: LaneFilters

  """Matches rows whose toLane matches these filters"""
  toLane: LaneFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [CardEventFilters!]

  """Every branch matches"""
  AND: [CardEventFilters!]

  """Negates the nested filters"""
  NOT: CardEventFilters
}

input CardEventsActorEnumFilter {
  """Equal to"""
  eq: CardEventsActorEnum

  """Not equal to"""
  ne: CardEventsActorEnum

  """Less than"""
  lt: CardEventsActorEnum

  """Less than or equal to"""
  lte: CardEventsActorEnum

  """Greater than"""
  gt: CardEventsActorEnum

  """Greater than or equal to"""
  gte: CardEventsActorEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [CardEventsActorEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [CardEventsActorEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [CardEventsActorEnumFilter!]

  """Every branch matches"""
  AND: [CardEventsActorEnumFilter!]

  """Negates the nested operators"""
  NOT: CardEventsActorEnumFilter
}

input CardEventOrderBy {
  id: InnerOrder
  cardId: InnerOrder
  runId: InnerOrder
  fromLaneId: InnerOrder
  toLaneId: InnerOrder
  noteId: InnerOrder
  actor: InnerOrder
  createdAt: InnerOrder

  """Order by columns of the related card row"""
  card: CardOrderBy

  """Order by columns of the related run row"""
  run: RunOrderBy

  """Order by columns of the related note row"""
  note: CardNoteOrderBy

  """Order by columns of the related fromLane row"""
  fromLane: LaneOrderBy

  """Order by columns of the related toLane row"""
  toLane: LaneOrderBy
}

"""Filters CardEvent groups by their aggregated values"""
input CardEventHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: CardEventCountNonNullHaving
  countDistinct: CardEventCountDistinctHaving
}

input CardEventCountNonNullHaving {
  id: AggregateNumberFilter
  cardId: AggregateNumberFilter
  runId: AggregateNumberFilter
  fromLaneId: AggregateNumberFilter
  toLaneId: AggregateNumberFilter
  noteId: AggregateNumberFilter
  actor: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input CardEventCountDistinctHaving {
  id: AggregateNumberFilter
  cardId: AggregateNumberFilter
  runId: AggregateNumberFilter
  fromLaneId: AggregateNumberFilter
  toLaneId: AggregateNumberFilter
  noteId: AggregateNumberFilter
  actor: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input RunFilters {
  id: StringFilter
  projectId: StringFilter
  agentId: StringFilter
  kind: RunsKindEnumFilter
  taskId: StringFilter
  cardId: StringFilter
  laneId: StringFilter
  status: RunsStatusEnumFilter
  verdict: RunsVerdictEnumFilter
  startedAt: DateTimeFilter
  finishedAt: DateTimeFilter
  output: StringFilter
  error: StringFilter
  toolCalls: JSONFilter
  promptTokens: IntFilter
  completionTokens: IntFilter
  totalTokens: IntFilter

  """Matches rows whose project matches these filters"""
  project: ProjectFilters

  """Matches rows whose agent matches these filters"""
  agent: AgentFilters

  """Matches rows whose task matches these filters"""
  task: TaskFilters

  """Matches rows whose card matches these filters"""
  card: CardFilters

  """Matches rows whose lane matches these filters"""
  lane: LaneFilters

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [RunFilters!]

  """Every branch matches"""
  AND: [RunFilters!]

  """Negates the nested filters"""
  NOT: RunFilters
}

input RunsKindEnumFilter {
  """Equal to"""
  eq: RunsKindEnum

  """Not equal to"""
  ne: RunsKindEnum

  """Less than"""
  lt: RunsKindEnum

  """Less than or equal to"""
  lte: RunsKindEnum

  """Greater than"""
  gt: RunsKindEnum

  """Greater than or equal to"""
  gte: RunsKindEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [RunsKindEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [RunsKindEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [RunsKindEnumFilter!]

  """Every branch matches"""
  AND: [RunsKindEnumFilter!]

  """Negates the nested operators"""
  NOT: RunsKindEnumFilter
}

input RunsStatusEnumFilter {
  """Equal to"""
  eq: RunsStatusEnum

  """Not equal to"""
  ne: RunsStatusEnum

  """Less than"""
  lt: RunsStatusEnum

  """Less than or equal to"""
  lte: RunsStatusEnum

  """Greater than"""
  gt: RunsStatusEnum

  """Greater than or equal to"""
  gte: RunsStatusEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [RunsStatusEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [RunsStatusEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [RunsStatusEnumFilter!]

  """Every branch matches"""
  AND: [RunsStatusEnumFilter!]

  """Negates the nested operators"""
  NOT: RunsStatusEnumFilter
}

input RunsVerdictEnumFilter {
  """Equal to"""
  eq: RunsVerdictEnum

  """Not equal to"""
  ne: RunsVerdictEnum

  """Less than"""
  lt: RunsVerdictEnum

  """Less than or equal to"""
  lte: RunsVerdictEnum

  """Greater than"""
  gt: RunsVerdictEnum

  """Greater than or equal to"""
  gte: RunsVerdictEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [RunsVerdictEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [RunsVerdictEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [RunsVerdictEnumFilter!]

  """Every branch matches"""
  AND: [RunsVerdictEnumFilter!]

  """Negates the nested operators"""
  NOT: RunsVerdictEnumFilter
}

input RunOrderBy {
  id: InnerOrder
  projectId: InnerOrder
  agentId: InnerOrder
  kind: InnerOrder
  taskId: InnerOrder
  cardId: InnerOrder
  laneId: InnerOrder
  status: InnerOrder
  verdict: InnerOrder
  startedAt: InnerOrder
  finishedAt: InnerOrder
  output: InnerOrder
  error: InnerOrder
  toolCalls: InnerOrder
  promptTokens: InnerOrder
  completionTokens: InnerOrder
  totalTokens: InnerOrder

  """Order by columns of the related project row"""
  project: ProjectOrderBy

  """Order by columns of the related agent row"""
  agent: AgentOrderBy

  """Order by columns of the related task row"""
  task: TaskOrderBy

  """Order by columns of the related card row"""
  card: CardOrderBy

  """Order by columns of the related lane row"""
  lane: LaneOrderBy
}

"""Filters Run groups by their aggregated values"""
input RunHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  avg: RunAvgHaving
  sum: RunSumHaving
  min: RunMinHaving
  max: RunMaxHaving
  countNonNull: RunCountNonNullHaving
  countDistinct: RunCountDistinctHaving
}

input RunAvgHaving {
  promptTokens: AggregateNumberFilter
  completionTokens: AggregateNumberFilter
  totalTokens: AggregateNumberFilter
}

input RunSumHaving {
  promptTokens: AggregateNumberFilter
  completionTokens: AggregateNumberFilter
  totalTokens: AggregateNumberFilter
}

input RunMinHaving {
  promptTokens: AggregateNumberFilter
  completionTokens: AggregateNumberFilter
  totalTokens: AggregateNumberFilter
}

input RunMaxHaving {
  promptTokens: AggregateNumberFilter
  completionTokens: AggregateNumberFilter
  totalTokens: AggregateNumberFilter
}

input RunCountNonNullHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  agentId: AggregateNumberFilter
  kind: AggregateNumberFilter
  taskId: AggregateNumberFilter
  cardId: AggregateNumberFilter
  laneId: AggregateNumberFilter
  status: AggregateNumberFilter
  verdict: AggregateNumberFilter
  startedAt: AggregateNumberFilter
  finishedAt: AggregateNumberFilter
  output: AggregateNumberFilter
  error: AggregateNumberFilter
  toolCalls: AggregateNumberFilter
  promptTokens: AggregateNumberFilter
  completionTokens: AggregateNumberFilter
  totalTokens: AggregateNumberFilter
}

input RunCountDistinctHaving {
  id: AggregateNumberFilter
  projectId: AggregateNumberFilter
  agentId: AggregateNumberFilter
  kind: AggregateNumberFilter
  taskId: AggregateNumberFilter
  cardId: AggregateNumberFilter
  laneId: AggregateNumberFilter
  status: AggregateNumberFilter
  verdict: AggregateNumberFilter
  startedAt: AggregateNumberFilter
  finishedAt: AggregateNumberFilter
  output: AggregateNumberFilter
  error: AggregateNumberFilter
  promptTokens: AggregateNumberFilter
  completionTokens: AggregateNumberFilter
  totalTokens: AggregateNumberFilter
}

input BoardTemplateFilters {
  id: StringFilter
  name: StringFilter
  description: StringFilter
  lanes: JSONFilter
  createdAt: DateTimeFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [BoardTemplateFilters!]

  """Every branch matches"""
  AND: [BoardTemplateFilters!]

  """Negates the nested filters"""
  NOT: BoardTemplateFilters
}

input BoardTemplateOrderBy {
  id: InnerOrder
  name: InnerOrder
  description: InnerOrder
  lanes: InnerOrder
  createdAt: InnerOrder
}

"""Filters BoardTemplate groups by their aggregated values"""
input BoardTemplateHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  countNonNull: BoardTemplateCountNonNullHaving
  countDistinct: BoardTemplateCountDistinctHaving
}

input BoardTemplateCountNonNullHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  description: AggregateNumberFilter
  lanes: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input BoardTemplateCountDistinctHaving {
  id: AggregateNumberFilter
  name: AggregateNumberFilter
  description: AggregateNumberFilter
  createdAt: AggregateNumberFilter
}

input UpdateSettingInput {
  id: String
  baseUrl: String
  model: String
  maxTokens: Int
  temperature: Float
  maxToolIterations: Int
  toolDiscovery: SettingsToolDiscoveryEnum
  toolSelectModel: String
  requestTimeoutSeconds: Int
  maxRetries: Int
  runRetentionDays: Int
  workerIntervalSeconds: Int
  refineAgentId: String
  refinePrompt: String
}

"""
One entry of a batch update of Setting: the rows `where` matches get this entry's `set` applied.
"""
input UpdateSettingManyInput {
  """Rows this entry updates. An omitted filter updates every row."""
  where: SettingFilters
  set: UpdateSettingInput!
}

input SettingFilters {
  id: StringFilter
  baseUrl: StringFilter
  model: StringFilter
  maxTokens: IntFilter
  temperature: FloatFilter
  maxToolIterations: IntFilter
  toolDiscovery: SettingsToolDiscoveryEnumFilter
  toolSelectModel: StringFilter
  requestTimeoutSeconds: IntFilter
  maxRetries: IntFilter
  runRetentionDays: IntFilter
  workerIntervalSeconds: IntFilter
  refineAgentId: StringFilter
  refinePrompt: StringFilter

  """At least one branch matches; ANDed with any sibling fields"""
  OR: [SettingFilters!]

  """Every branch matches"""
  AND: [SettingFilters!]

  """Negates the nested filters"""
  NOT: SettingFilters
}

input SettingsToolDiscoveryEnumFilter {
  """Equal to"""
  eq: SettingsToolDiscoveryEnum

  """Not equal to"""
  ne: SettingsToolDiscoveryEnum

  """Less than"""
  lt: SettingsToolDiscoveryEnum

  """Less than or equal to"""
  lte: SettingsToolDiscoveryEnum

  """Greater than"""
  gt: SettingsToolDiscoveryEnum

  """Greater than or equal to"""
  gte: SettingsToolDiscoveryEnum
  like: String
  notLike: String
  ilike: String
  notIlike: String

  """
  Matches values starting with the given string. `%`, `_` and `\` are matched literally.
  """
  startsWith: String

  """
  Matches values ending with the given string. `%`, `_` and `\` are matched literally.
  """
  endsWith: String

  """
  Matches values containing the given string. `%`, `_` and `\` are matched literally.
  """
  contains: String

  """Case-insensitive `startsWith`."""
  iStartsWith: String

  """Case-insensitive `endsWith`."""
  iEndsWith: String

  """Case-insensitive `contains`."""
  iContains: String

  """
  When true, every comparison operator in this object matches case-insensitively — `eq`, `ne`, the ordering operators, `inArray`/`notInArray` and the pattern operators all compare `lower(column)` against `lower(operand)`. Applies only to the operators beside it; a nested `AND`/`OR`/`NOT` branch sets its own.
  """
  insensitive: Boolean

  """Matches any one of these values (SQL `IN`)"""
  inArray: [SettingsToolDiscoveryEnum!]

  """Matches none of these values (SQL `NOT IN`)"""
  notInArray: [SettingsToolDiscoveryEnum!]

  """When true, matches rows where the column is NULL"""
  isNull: Boolean

  """When true, matches rows where the column is not NULL"""
  isNotNull: Boolean

  """At least one branch matches; ANDed with any sibling operators"""
  OR: [SettingsToolDiscoveryEnumFilter!]

  """Every branch matches"""
  AND: [SettingsToolDiscoveryEnumFilter!]

  """Negates the nested operators"""
  NOT: SettingsToolDiscoveryEnumFilter
}

input SettingOrderBy {
  id: InnerOrder
  baseUrl: InnerOrder
  model: InnerOrder
  maxTokens: InnerOrder
  temperature: InnerOrder
  maxToolIterations: InnerOrder
  toolDiscovery: InnerOrder
  toolSelectModel: InnerOrder
  requestTimeoutSeconds: InnerOrder
  maxRetries: InnerOrder
  runRetentionDays: InnerOrder
  workerIntervalSeconds: InnerOrder
  refineAgentId: InnerOrder
  refinePrompt: InnerOrder
}

"""Filters Setting groups by their aggregated values"""
input SettingHaving {
  """Filters groups by how many rows they contain"""
  count: AggregateNumberFilter
  avg: SettingAvgHaving
  sum: SettingSumHaving
  min: SettingMinHaving
  max: SettingMaxHaving
  countNonNull: SettingCountNonNullHaving
  countDistinct: SettingCountDistinctHaving
}

input SettingAvgHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  runRetentionDays: AggregateNumberFilter
  workerIntervalSeconds: AggregateNumberFilter
}

input SettingSumHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  runRetentionDays: AggregateNumberFilter
  workerIntervalSeconds: AggregateNumberFilter
}

input SettingMinHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  runRetentionDays: AggregateNumberFilter
  workerIntervalSeconds: AggregateNumberFilter
}

input SettingMaxHaving {
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  runRetentionDays: AggregateNumberFilter
  workerIntervalSeconds: AggregateNumberFilter
}

input SettingCountNonNullHaving {
  id: AggregateNumberFilter
  baseUrl: AggregateNumberFilter
  model: AggregateNumberFilter
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  toolDiscovery: AggregateNumberFilter
  toolSelectModel: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  runRetentionDays: AggregateNumberFilter
  workerIntervalSeconds: AggregateNumberFilter
  refineAgentId: AggregateNumberFilter
  refinePrompt: AggregateNumberFilter
}

input SettingCountDistinctHaving {
  id: AggregateNumberFilter
  baseUrl: AggregateNumberFilter
  model: AggregateNumberFilter
  maxTokens: AggregateNumberFilter
  temperature: AggregateNumberFilter
  maxToolIterations: AggregateNumberFilter
  toolDiscovery: AggregateNumberFilter
  toolSelectModel: AggregateNumberFilter
  requestTimeoutSeconds: AggregateNumberFilter
  maxRetries: AggregateNumberFilter
  runRetentionDays: AggregateNumberFilter
  workerIntervalSeconds: AggregateNumberFilter
  refineAgentId: AggregateNumberFilter
  refinePrompt: AggregateNumberFilter
}

type Query {
  roles(
    offset: Int
    limit: Int
    orderBy: RoleOrderBy
    where: RoleFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [RoleDistinctColumn!]
  ): [Role!]!
  role(offset: Int, orderBy: RoleOrderBy, where: RoleFilters): Role
  rolesAggregate(where: RoleFilters): RoleAggregate!
  rolesGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [RoleGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: RoleFilters

    """Filters the groups after they are aggregated."""
    having: RoleHaving
  ): [RoleGroupBy!]!
  agents(
    offset: Int
    limit: Int
    orderBy: AgentOrderBy
    where: AgentFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [AgentDistinctColumn!]
  ): [Agent!]!
  agent(offset: Int, orderBy: AgentOrderBy, where: AgentFilters): Agent
  agentsAggregate(where: AgentFilters): AgentAggregate!
  agentsGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [AgentGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: AgentFilters

    """Filters the groups after they are aggregated."""
    having: AgentHaving
  ): [AgentGroupBy!]!
  mcpServers(
    offset: Int
    limit: Int
    orderBy: McpServerOrderBy
    where: McpServerFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [McpServerDistinctColumn!]
  ): [McpServer!]!
  mcpServer(offset: Int, orderBy: McpServerOrderBy, where: McpServerFilters): McpServer
  mcpServersAggregate(where: McpServerFilters): McpServerAggregate!
  mcpServersGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [McpServerGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: McpServerFilters

    """Filters the groups after they are aggregated."""
    having: McpServerHaving
  ): [McpServerGroupBy!]!
  agentServers(
    offset: Int
    limit: Int
    orderBy: AgentServerOrderBy
    where: AgentServerFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [AgentServerDistinctColumn!]
  ): [AgentServer!]!
  agentServer(offset: Int, orderBy: AgentServerOrderBy, where: AgentServerFilters): AgentServer
  agentServersAggregate(where: AgentServerFilters): AgentServerAggregate!
  agentServersGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [AgentServerGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: AgentServerFilters

    """Filters the groups after they are aggregated."""
    having: AgentServerHaving
  ): [AgentServerGroupBy!]!
  projects(
    offset: Int
    limit: Int
    orderBy: ProjectOrderBy
    where: ProjectFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [ProjectDistinctColumn!]
  ): [Project!]!
  project(offset: Int, orderBy: ProjectOrderBy, where: ProjectFilters): Project
  projectsAggregate(where: ProjectFilters): ProjectAggregate!
  projectsGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [ProjectGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: ProjectFilters

    """Filters the groups after they are aggregated."""
    having: ProjectHaving
  ): [ProjectGroupBy!]!
  lanes(
    offset: Int
    limit: Int
    orderBy: LaneOrderBy
    where: LaneFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [LaneDistinctColumn!]
  ): [Lane!]!
  lane(offset: Int, orderBy: LaneOrderBy, where: LaneFilters): Lane
  lanesAggregate(where: LaneFilters): LaneAggregate!
  lanesGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [LaneGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: LaneFilters

    """Filters the groups after they are aggregated."""
    having: LaneHaving
  ): [LaneGroupBy!]!
  tasks(
    offset: Int
    limit: Int
    orderBy: TaskOrderBy
    where: TaskFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [TaskDistinctColumn!]
  ): [Task!]!
  task(offset: Int, orderBy: TaskOrderBy, where: TaskFilters): Task
  tasksAggregate(where: TaskFilters): TaskAggregate!
  tasksGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [TaskGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: TaskFilters

    """Filters the groups after they are aggregated."""
    having: TaskHaving
  ): [TaskGroupBy!]!
  messages(
    offset: Int
    limit: Int
    orderBy: MessageOrderBy
    where: MessageFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [MessageDistinctColumn!]
  ): [Message!]!
  message(offset: Int, orderBy: MessageOrderBy, where: MessageFilters): Message
  messagesAggregate(where: MessageFilters): MessageAggregate!
  messagesGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [MessageGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: MessageFilters

    """Filters the groups after they are aggregated."""
    having: MessageHaving
  ): [MessageGroupBy!]!
  cards(
    offset: Int
    limit: Int
    orderBy: CardOrderBy
    where: CardFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [CardDistinctColumn!]
  ): [Card!]!
  card(offset: Int, orderBy: CardOrderBy, where: CardFilters): Card
  cardsAggregate(where: CardFilters): CardAggregate!
  cardsGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [CardGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: CardFilters

    """Filters the groups after they are aggregated."""
    having: CardHaving
  ): [CardGroupBy!]!
  cardDeps(
    offset: Int
    limit: Int
    orderBy: CardDepOrderBy
    where: CardDepFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [CardDepDistinctColumn!]
  ): [CardDep!]!
  cardDep(offset: Int, orderBy: CardDepOrderBy, where: CardDepFilters): CardDep
  cardDepsAggregate(where: CardDepFilters): CardDepAggregate!
  cardDepsGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [CardDepGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: CardDepFilters

    """Filters the groups after they are aggregated."""
    having: CardDepHaving
  ): [CardDepGroupBy!]!
  cardNotes(
    offset: Int
    limit: Int
    orderBy: CardNoteOrderBy
    where: CardNoteFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [CardNoteDistinctColumn!]
  ): [CardNote!]!
  cardNote(offset: Int, orderBy: CardNoteOrderBy, where: CardNoteFilters): CardNote
  cardNotesAggregate(where: CardNoteFilters): CardNoteAggregate!
  cardNotesGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [CardNoteGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: CardNoteFilters

    """Filters the groups after they are aggregated."""
    having: CardNoteHaving
  ): [CardNoteGroupBy!]!
  cardEvents(
    offset: Int
    limit: Int
    orderBy: CardEventOrderBy
    where: CardEventFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [CardEventDistinctColumn!]
  ): [CardEvent!]!
  cardEvent(offset: Int, orderBy: CardEventOrderBy, where: CardEventFilters): CardEvent
  cardEventsAggregate(where: CardEventFilters): CardEventAggregate!
  cardEventsGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [CardEventGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: CardEventFilters

    """Filters the groups after they are aggregated."""
    having: CardEventHaving
  ): [CardEventGroupBy!]!
  runs(
    offset: Int
    limit: Int
    orderBy: RunOrderBy
    where: RunFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [RunDistinctColumn!]
  ): [Run!]!
  run(offset: Int, orderBy: RunOrderBy, where: RunFilters): Run
  runsAggregate(where: RunFilters): RunAggregate!
  runsGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [RunGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: RunFilters

    """Filters the groups after they are aggregated."""
    having: RunHaving
  ): [RunGroupBy!]!
  boardTemplates(
    offset: Int
    limit: Int
    orderBy: BoardTemplateOrderBy
    where: BoardTemplateFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [BoardTemplateDistinctColumn!]
  ): [BoardTemplate!]!
  boardTemplate(offset: Int, orderBy: BoardTemplateOrderBy, where: BoardTemplateFilters): BoardTemplate
  boardTemplatesAggregate(where: BoardTemplateFilters): BoardTemplateAggregate!
  boardTemplatesGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [BoardTemplateGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: BoardTemplateFilters

    """Filters the groups after they are aggregated."""
    having: BoardTemplateHaving
  ): [BoardTemplateGroupBy!]!
  settings(
    offset: Int
    limit: Int
    orderBy: SettingOrderBy
    where: SettingFilters

    """
    Keyset pagination: only return rows strictly after this cursor (a row's `cursor` field from a previous page, under the same orderBy).
    """
    after: String
    distinct: [SettingDistinctColumn!]
  ): [Setting!]!
  setting(offset: Int, orderBy: SettingOrderBy, where: SettingFilters): Setting
  settingsAggregate(where: SettingFilters): SettingAggregate!
  settingsGroupBy(
    """
    Columns to group by. One result row per distinct combination of their values.
    """
    groupBy: [SettingGroupByColumn!]!

    """Filters the rows before they are grouped."""
    where: SettingFilters

    """Filters the groups after they are aggregated."""
    having: SettingHaving
  ): [SettingGroupBy!]!

  """
  Model ids an OpenAI-compatible server reports. With no `agentId` this asks the endpoint in Settings; with one it asks that agent's own endpoint, which is the list that agent can actually choose from.
  """
  models(agentId: String): [String!]!

  """
  The cards this one is waiting on that are not finished yet — the reason a card sits in a lane its agent never picks it up from. Empty means nothing is in its way. Worked out from the cards as they stand every time it is asked, rather than read off the card, because the answer changes when some other card finishes and nothing would go back to rewrite it. An archived dependency is not in the way: taking one off the board is a decision that it does not have to happen.
  """
  blockers(cardId: String!): [Card!]!

  """
  Which of the configured MCP servers this one actually reached, and the tools it found on each. A server that is enabled but absent here failed to connect, and its tools are not offered to any agent linked to it.
  """
  mcpStatus: [McpServerStatus!]!

  """
  What a run has said so far, oldest first, with consecutive thinking and output tokens folded into one entry each. The snapshot form of the `runEvents` subscription, for a client that polls rather than holds a stream open: pass the `seq` of the last entry you read as `afterSeq` to pick up exactly where you left off. Empty for a run that has not started, or one that finished over a minute ago.
  """
  runEvents(
    runId: String!

    """Only what is numbered above this. Omit for the whole run."""
    afterSeq: Int

    """At most this many entries, oldest first. Default 200."""
    limit: Int
  ): [RunEvent!]!

  """
  What a project has cost in tokens, over a window, added up from its runs. With a `taskId` it is that one task instead: its refinement, its decomposition, and every run of every card it turned into. Read `from` before quoting the number — it says how far back the runs behind it actually go.
  """
  spend(
    projectId: String!

    """
    Narrows the total to one task of that project. Omit for the whole board.
    """
    taskId: String

    """How far back to count. Default 30. Zero counts every run still kept."""
    days: Int
  ): Spend!
}

"""Columns of Role that a query can be made distinct on"""
enum RoleDistinctColumn {
  id
  name
  description
  contract
  prompt
  createdAt
  updatedAt
}

"""Columns of Role that a query can group by"""
enum RoleGroupByColumn {
  id
  name
  description
  contract
  prompt
  createdAt
  updatedAt
}

"""Columns of Agent that a query can be made distinct on"""
enum AgentDistinctColumn {
  id
  name
  enabled
  baseUrl
  model
  systemPrompt
  maxTokens
  temperature
  maxToolIterations
  toolDiscovery
  requestTimeoutSeconds
  maxRetries
  createdAt
  updatedAt
}

"""Columns of Agent that a query can group by"""
enum AgentGroupByColumn {
  id
  name
  enabled
  baseUrl
  model
  systemPrompt
  maxTokens
  temperature
  maxToolIterations
  toolDiscovery
  requestTimeoutSeconds
  maxRetries
  createdAt
  updatedAt
}

"""Columns of McpServer that a query can be made distinct on"""
enum McpServerDistinctColumn {
  id
  slug
  label
  enabled
  transport
  command
  args
  env
  url
  headers
}

"""Columns of McpServer that a query can group by"""
enum McpServerGroupByColumn {
  id
  slug
  label
  enabled
  transport
  command
  url
}

"""Columns of AgentServer that a query can group by"""
enum AgentServerGroupByColumn {
  id
  agentId
  serverId
}

"""Columns of Project that a query can be made distinct on"""
enum ProjectDistinctColumn {
  id
  name
  description
  context
  autoRun
  refineAgentId
  createdAt
  updatedAt
}

"""Columns of Project that a query can group by"""
enum ProjectGroupByColumn {
  id
  name
  description
  context
  autoRun
  refineAgentId
  createdAt
  updatedAt
}

"""Columns of Lane that a query can group by"""
enum LaneGroupByColumn {
  id
  projectId
  name
  position
  intake
  roleId
  prompt
  agentId
  onSuccessLaneId
  archiveOnSuccess
  onFailureLaneId
  wipLimit
  maxAttempts
  createdAt
}

"""Columns of Task that a query can group by"""
enum TaskGroupByColumn {
  id
  projectId
  title
  brief
  createdAt
  updatedAt
}

"""Columns of Message that a query can group by"""
enum MessageGroupByColumn {
  id
  taskId
  role
  content
  createdAt
}

"""Columns of Card that a query can group by"""
enum CardGroupByColumn {
  id
  projectId
  taskId
  parentId
  laneId
  title
  body
  acceptance
  position
  status
  error
  attempts
  archivedAt
  createdAt
  updatedAt
}

"""Columns of CardDep that a query can group by"""
enum CardDepGroupByColumn {
  id
  cardId
  dependsOnCardId
}

"""Columns of CardNote that a query can group by"""
enum CardNoteGroupByColumn {
  id
  cardId
  runId
  kind
  author
  body
  createdAt
  updatedAt
}

"""Columns of CardEvent that a query can group by"""
enum CardEventGroupByColumn {
  id
  cardId
  runId
  fromLaneId
  toLaneId
  noteId
  actor
  createdAt
}

"""Columns of Run that a query can group by"""
enum RunGroupByColumn {
  id
  projectId
  agentId
  kind
  taskId
  cardId
  laneId
  status
  verdict
  startedAt
  finishedAt
  output
  error
  promptTokens
  completionTokens
  totalTokens
}

"""Columns of BoardTemplate that a query can be made distinct on"""
enum BoardTemplateDistinctColumn {
  id
  name
  description
  lanes
  createdAt
}

"""Columns of BoardTemplate that a query can group by"""
enum BoardTemplateGroupByColumn {
  id
  name
  description
  createdAt
}

"""Columns of Setting that a query can be made distinct on"""
enum SettingDistinctColumn {
  id
  baseUrl
  model
  maxTokens
  temperature
  maxToolIterations
  toolDiscovery
  toolSelectModel
  requestTimeoutSeconds
  maxRetries
  runRetentionDays
  workerIntervalSeconds
  refineAgentId
  refinePrompt
}

"""Columns of Setting that a query can group by"""
enum SettingGroupByColumn {
  id
  baseUrl
  model
  maxTokens
  temperature
  maxToolIterations
  toolDiscovery
  toolSelectModel
  requestTimeoutSeconds
  maxRetries
  runRetentionDays
  workerIntervalSeconds
  refineAgentId
  refinePrompt
}

"""
Live connection state for a configured MCP server, and the tools it offers.
"""
type McpServerStatus {
  id: String!
  slug: String!
  label: String!
  status: String!
  error: String!
  tools: [McpTool!]!
}

type McpTool {
  name: String!
  description: String!
}

"""
Something a run did while it was running — a token, a tool call, a notice. Held in memory for the length of the run and a minute after; the run row is the lasting record.
"""
type RunEvent {
  runId: String!

  """Per-run counter from 1, so a client can order and de-duplicate."""
  seq: Int!
  at: DateTime!

  """turn | thinking | output | tool-call | tool-result | notice | done."""
  kind: String!
  text: String!

  """Tool name, where there is one."""
  name: String!
  ok: Boolean
}

"""
What has been spent on a project, or on one task in it, added up from the run rows themselves rather than from a counter — a counter would keep climbing after retention deleted the runs behind it.
"""
type Spend {
  """How many runs went into this total."""
  runs: Int!
  promptTokens: Int!
  completionTokens: Int!
  totalTokens: Int!

  """The window that was asked for. Zero means every run still kept."""
  days: Int!

  """
  The oldest run in the total. This, not `days`, is what the number actually covers: if retention has swept older runs away, it is later than the window asked for. Null when nothing was counted.
  """
  from: DateTime

  """
  How long runs are kept, from settings. Zero means forever, and then the total is the whole history.
  """
  retentionDays: Int!
}

type Mutation {
  createRoles(values: [CreateRoleInput!]!): [Role!]!
  createRole(values: CreateRoleInput!): Role!
  updateRole(set: UpdateRoleInput!, where: RoleFilters): [Role!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateRolesMany(updates: [UpdateRoleManyInput!]!): [Role]!
  updateRoleSingle(set: UpdateRoleInput!, where: RoleFilters!): Role
  deleteRole(where: RoleFilters): [Role!]!
  deleteRoleSingle(where: RoleFilters!): Role
  createAgents(values: [CreateAgentInput!]!): [Agent!]!
  createAgent(values: CreateAgentInput!): Agent!
  updateAgent(set: UpdateAgentInput!, where: AgentFilters): [Agent!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateAgentsMany(updates: [UpdateAgentManyInput!]!): [Agent]!
  updateAgentSingle(set: UpdateAgentInput!, where: AgentFilters!): Agent
  deleteAgent(where: AgentFilters): [Agent!]!
  deleteAgentSingle(where: AgentFilters!): Agent
  createMcpServers(values: [CreateMcpServerInput!]!): [McpServer!]!
  createMcpServer(values: CreateMcpServerInput!): McpServer!
  updateMcpServer(set: UpdateMcpServerInput!, where: McpServerFilters): [McpServer!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateMcpServersMany(updates: [UpdateMcpServerManyInput!]!): [McpServer]!
  updateMcpServerSingle(set: UpdateMcpServerInput!, where: McpServerFilters!): McpServer
  deleteMcpServer(where: McpServerFilters): [McpServer!]!
  deleteMcpServerSingle(where: McpServerFilters!): McpServer
  createAgentServers(values: [CreateAgentServerInput!]!): [AgentServer!]!
  createAgentServer(values: CreateAgentServerInput!): AgentServer!
  updateAgentServer(set: UpdateAgentServerInput!, where: AgentServerFilters): [AgentServer!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateAgentServersMany(updates: [UpdateAgentServerManyInput!]!): [AgentServer]!
  updateAgentServerSingle(set: UpdateAgentServerInput!, where: AgentServerFilters!): AgentServer
  deleteAgentServer(where: AgentServerFilters): [AgentServer!]!
  deleteAgentServerSingle(where: AgentServerFilters!): AgentServer
  createProjects(values: [CreateProjectInput!]!): [Project!]!
  createProject(values: CreateProjectInput!): Project!
  updateProject(set: UpdateProjectInput!, where: ProjectFilters): [Project!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateProjectsMany(updates: [UpdateProjectManyInput!]!): [Project]!
  updateProjectSingle(set: UpdateProjectInput!, where: ProjectFilters!): Project
  deleteProject(where: ProjectFilters): [Project!]!
  deleteProjectSingle(where: ProjectFilters!): Project
  createLanes(values: [CreateLaneInput!]!): [Lane!]!
  createLane(values: CreateLaneInput!): Lane!
  updateLane(set: UpdateLaneInput!, where: LaneFilters): [Lane!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateLanesMany(updates: [UpdateLaneManyInput!]!): [Lane]!
  updateLaneSingle(set: UpdateLaneInput!, where: LaneFilters!): Lane
  deleteLane(where: LaneFilters): [Lane!]!
  deleteLaneSingle(where: LaneFilters!): Lane
  createTasks(values: [CreateTaskInput!]!): [Task!]!
  createTask(values: CreateTaskInput!): Task!
  updateTask(set: UpdateTaskInput!, where: TaskFilters): [Task!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateTasksMany(updates: [UpdateTaskManyInput!]!): [Task]!
  updateTaskSingle(set: UpdateTaskInput!, where: TaskFilters!): Task
  deleteTask(where: TaskFilters): [Task!]!
  deleteTaskSingle(where: TaskFilters!): Task
  createMessages(values: [CreateMessageInput!]!): [Message!]!
  createMessage(values: CreateMessageInput!): Message!
  updateMessage(set: UpdateMessageInput!, where: MessageFilters): [Message!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateMessagesMany(updates: [UpdateMessageManyInput!]!): [Message]!
  updateMessageSingle(set: UpdateMessageInput!, where: MessageFilters!): Message
  deleteMessage(where: MessageFilters): [Message!]!
  deleteMessageSingle(where: MessageFilters!): Message
  createCards(values: [CreateCardInput!]!): [Card!]!
  createCard(values: CreateCardInput!): Card!
  updateCard(set: UpdateCardInput!, where: CardFilters): [Card!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateCardsMany(updates: [UpdateCardManyInput!]!): [Card]!
  updateCardSingle(set: UpdateCardInput!, where: CardFilters!): Card
  deleteCard(where: CardFilters): [Card!]!
  deleteCardSingle(where: CardFilters!): Card
  createCardDeps(values: [CreateCardDepInput!]!): [CardDep!]!
  createCardDep(values: CreateCardDepInput!): CardDep!
  updateCardDep(set: UpdateCardDepInput!, where: CardDepFilters): [CardDep!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateCardDepsMany(updates: [UpdateCardDepManyInput!]!): [CardDep]!
  updateCardDepSingle(set: UpdateCardDepInput!, where: CardDepFilters!): CardDep
  deleteCardDep(where: CardDepFilters): [CardDep!]!
  deleteCardDepSingle(where: CardDepFilters!): CardDep
  deleteRun(where: RunFilters): [Run!]!
  deleteRunSingle(where: RunFilters!): Run
  deleteBoardTemplate(where: BoardTemplateFilters): [BoardTemplate!]!
  deleteBoardTemplateSingle(where: BoardTemplateFilters!): BoardTemplate
  updateSetting(set: UpdateSettingInput!, where: SettingFilters): [Setting!]!

  """
  Each entry's updated rows, in entry order. An entry whose `where` matched no rows contributes `null` in its slot; an entry that matched several contributes each of its rows.
  """
  updateSettingsMany(updates: [UpdateSettingManyInput!]!): [Setting]!
  updateSettingSingle(set: UpdateSettingInput!, where: SettingFilters!): Setting

  """
  One turn of talking a task into shape: says something to the refining agent and resolves once it has answered. The answer is appended to the task's messages and the task's title and brief are rewritten from it, so read the task back after this to see where the brief has got to. A task can be talked about for as long as you like; `makeCard` is what ends the conversation by putting it on the board.
  """
  refineTask(taskId: String!, message: String!): Run!

  """
  Ends a refining conversation by putting it on the board: writes the task's title and brief as one card in the project's intake lane, linked back to the task. It is one card and not many — breaking work up is a station on the board now, so a card landing in a lane that expands is what turns it into the cards that carry it out. The conversation is left exactly where it is and can go on afterwards.
  """
  makeCard(taskId: String!): Card!

  """
  The way onto a board for a caller that has no lane ids: writes one card at the project's front door — the lane marked `intake`, else the leftmost — and answers with it. Use this rather than `create_card` unless you know exactly which lane you mean. If that lane is a station that expands, the card becomes the cards that carry the work out as soon as it is worked.
  """
  submitCard(
    projectId: String!
    title: String!

    """What is wanted, in as much detail as you have. An agent reads it."""
    body: String!
  ): Card!

  """
  Works one card now, with its lane's agent unless `agentId` names another, and resolves with the finished run — a long call for a long card. Read `runEvents` meanwhile to watch it, or `stopCard` to call it off. The card moves on afterwards exactly as it would have under `autoRun`, following its lane's success and failure arrows. Refused for a card waiting on unfinished dependencies.
  """
  runCard(cardId: String!, agentId: String): Run!

  """
  Calls off the agent working a card. False means none was — a stale button, not a failure. The run is recorded as `stopped` and the card goes back to `idle` where it is, rather than moving on.
  """
  stopCard(cardId: String!): Boolean!

  """Calls off a refinement in flight. False means none was running."""
  stopTask(taskId: String!): Boolean!

  """
  Puts a card in a lane, at a position, and renumbers the cards around it so the board stays in the order it looks like. Omit `position` to drop it at the end. A card that had failed comes back to `idle` with its attempts forgiven, which is what makes dragging one back a retry; a card an agent is working cannot be moved out from under it, and nor can an archived one — `restoreCard` is what puts that back on the board. Say why in `note` and the agent that picks the card up is told it, the same way a reviewer's rejection reaches one: moving a card back without saying what was wrong with it buys a second attempt identical to the first.
  """
  moveCard(cardId: String!, laneId: String!, position: Int, note: String): Card!

  """
  Puts a card back in play where it stands: clears its error, empties the count of failed attempts against it, and returns it to `idle`, which is the status a lane's agent will pick up. This is the way back for a card that stopped at `error` — one a reviewer rejected once its lane had no attempts left to spend, or one whose lane spends none. It does not run anything itself; `runCard` does that, and `autoRun` does it unasked. Refused while an agent is working the card, and on an archived one — `restoreCard` puts that back on the board first.
  """
  retryCard(cardId: String!): Card!

  """
  Takes a card off the board without deleting it. It stops being drawn in its lane, stops being picked up by that lane's agent, and stops counting as something other cards are waiting on — but it keeps its lane, its status and everything said about it, and `restoreCard` puts it back. This is what a Done pile is for once it is long enough to be in the way. Read the archive with `cards(where: { archivedAt: { isNotNull: true } })`. Refused while an agent is working the card; archiving one already archived leaves the time it was archived alone.
  """
  archiveCard(cardId: String!): Card!

  """
  Puts an archived card back on the board, at the end of the lane it was archived from. Its status is left as it was found — a card archived as `error` comes back as one, and `retryCard` is still the way to put that back in play — because what the card was is the reason someone archived it. The end of the lane rather than its old position, which the cards added since have long taken.
  """
  restoreCard(cardId: String!): Card!

  """
  Writes a note on a card. Everything ever said about a card is a note — the report an agent leaves when it has worked one, the verdict a reviewing station rules, and this, a standing note somebody wants taken into account. Every note on the card is handed to the next agent that works it, under "Notes on this card", so this is how you tell one something the card's body does not say without editing the card out from under whoever wrote it. Reports and verdicts are written by the runner and cannot be written here: a note says who wrote it, and that has to be true. Read them with `card_notes(where: { cardId: { eq: "…" } })`.
  """
  addCardNote(cardId: String!, body: String!): CardNote!

  """
  Rewrites a note. Only a note somebody wrote: an agent's report and a reviewer's verdict are an account of what happened, and an account that can be corrected afterwards is worth no more than no account at all.
  """
  updateCardNote(id: String!, body: String!): CardNote!

  """
  Takes a note back, so the next agent working the card stops being told it. Only a note somebody wrote, for the same reason `updateCardNote` is. A verdict that explained a move stays readable through that move in `card_events`.
  """
  deleteCardNote(id: String!): Boolean!

  """
  Replaces what a card waits on, and answers with it. A card with unfinished dependencies is skipped rather than run out of order, so this is how a decomposition that got the order wrong is corrected. Written as a set rather than a row at a time, because half an ordering is not an ordering. Every id has to be a card on the same board, and a cycle is refused — cards that wait on each other would never run, and nothing downstream would say why.
  """
  setCardDeps(cardId: String!, dependsOn: [String!]!): [String!]!

  """
  Keeps a project's board — its lanes, their agents, their WIP limits and the arrows between them — under a name, so the next project can start with it instead of it being drawn again. Saving under a name that already exists replaces it. The cards are not part of it: a template is the shape of a board, not its contents.
  """
  saveBoardTemplate(projectId: String!, name: String!, description: String): BoardTemplate!

  """
  Redraws a project's board from a saved template, and answers with the lanes it wrote. Refused once the board has cards on it, archived ones included: replacing lanes takes their cards with them, so this is for a project that has not started rather than a way to rearrange one that has.
  """
  applyBoardTemplate(projectId: String!, templateId: String!): [Lane!]!

  """
  Replaces the set of MCP servers an agent may reach, and answers with it. Written as a set rather than a row at a time because that is how it is decided — an agent's tools are the whole of what it can do, and half-applied is a different agent.
  """
  setAgentServers(agentId: String!, serverIds: [String!]!): [String!]!

  """
  Connects to a config that need not be saved yet and lists its tools, so a server can be checked before an agent depends on it.
  """
  testMcpServer(config: McpConnectionInput!): McpProbe!

  """Tears down and rebuilds every MCP connection."""
  reconnectMcp: [McpServerStatus!]!

  """
  Writes the shared API key. Separate from updateSetting because the key is write-only: it is excluded from the Setting type so it can never be read back out.
  """
  setApiKey(apiKey: String!): Boolean!

  """
  Writes one agent's own API key, for an agent pointed at an endpoint of its own. Write-only, like the shared one. Send an empty string to clear it — an agent with no key of its own borrows the shared one only while it is also on the shared endpoint.
  """
  setAgentApiKey(agentId: String!, apiKey: String!): Boolean!
}

"""
The result of dialling an MCP server once, without saving or pooling it.
"""
type McpProbe {
  ok: Boolean!
  error: String!
  tools: [McpTool!]!
}

"""
How to reach an MCP server — the connection half of a row, without its identity.
"""
input McpConnectionInput {
  transport: String!
  command: String
  args: [String!]
  env: JSON
  url: String
  headers: JSON
}

type Subscription {
  """
  Watches a run as it happens. Replays what the run has said so far, then follows it live, and completes when the run ends. Subscribing to a run that has not started waits for it; subscribing to one long finished ends straight away.
  """
  runEvents(runId: String!): RunEvent!
}